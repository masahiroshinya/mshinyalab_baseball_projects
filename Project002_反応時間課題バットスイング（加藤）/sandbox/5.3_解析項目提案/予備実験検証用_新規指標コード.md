# 5.5 予備実験（0604）データでの新規指標　検証用コード

**作成日**: 2026-07-03
**最終更新日**: 2026-07-03（各指標に適した可視化コードを追加）
**作成者**: 加藤 和太郎 (student) ／ AI提案
**位置づけ**: `技術説明.md` セクション9で提案した未実装項目のうち、**AIMを使わずバット剛体＋
フォースプレートのみで算出できる項目**を、既存の予備実験データ（0604、被験者S01・S02）で
動作検証するための**テストスクリプト案（未実行・未検証）**。各指標に適した可視化まで含む。

**可視化の方針**：指標の性質ごとに図の種類を変える。

- 大きさ（ピーク値・力積・RFD・MT・ヘッド速度）→ **条件別の箱ひげ図**（`x7_3` と同じスタイル）
- 時間的な出来事（体重移動・後方/合成GRF・力積の面積・ヘッド速度のピーク）→ **代表試行の時系列波形**
- 空間的な軌跡（CoP）→ **XY平面プロット（時間で色分け）**
- 変動性 → **試行番号 vs 値の散布図**（ばらつきの大きさを目視）
- 抑制課題 → **棒グラフ**（内訳・条件対比）

---

## 0. 本実験との違い（このコードで前提にしていること）

予備実験データは本実験と構成が異なるため、コードを以下のように予備実験向けに調整している。

| 項目           | 本実験（`技術説明.md` セクション9）        | 予備実験（本ファイル）                                           |
| -------------- | -------------------------------------------- | ---------------------------------------------------------------- |
| 条件           | 4条件（free/simple/gonogo/**gostop**） | **3条件（free/simple/gonogo）**。gostopは無い              |
| 各条件の試行数 | 10試行                                       | **20試行**（`size(DataArray,1)` で自動取得）             |
| Go:Nogo比率    | 7:3（Nogo 3試行）                            | 75:25（**Nogo 5試行/被験者**）                             |
| 被験者         | 20〜30名                                     | **S01・S02の2名のみ**                                      |
| 統計           | RM-ANOVA等                                   | 実施しない（**単一被験者の指標算出＋可視化の検証が目的**） |

> **したがって本コードの目的は「値が正しく計算できるか・単位や符号が妥当か」の確認**であり、
> 条件間の統計的比較や結論を出すためのものではない。特に9.8（抑制課題）はNogo試行が
> 少ないため、コードが動くことの確認に留める。

### 実行前提

- **MATLABのカレントフォルダを `Preliminary experiments/2026-0604/MATLAB/` にすること**
  （`x2_Data/` や `x5_...` を相対パスで読むため）。
- 依存関数（`parameters.m`・`filt_all_fields.m`・`diff3p.m`）は同フォルダに存在する。
- 実装済み項目（RT・ピーク角速度・ピークFz）のコードは含まない。

---

## 1. 共通の読み込みと前処理（すべてのSTEPの土台）

`x7_3_visualize_grf.m` と同じ読み込み方法。ここでは被験者を1名選んで検証する。

```matlab
clear
close all
clc

iSubject = 1 ;                                    % 検証する被験者（1 or 2）

% ---- データ読み込み ----
load(sprintf('x2_Data/Data%02d', iSubject))                                          % DataArray
load(sprintf('x5_SingleTrialAnalysisResultsChecked/SingleTrialAnalysisResults%02d', iSubject))  % SingleTrialResultArray

% ---- 予備実験は3条件 ----
ConditionNameArray = {'free', 'simple', 'gonogo'} ;
nCondition = length(ConditionNameArray) ;
nTrial     = size(DataArray, 1) ;                 % = 20

% ---- マーカーフィルタ用パラメータ（m3と同じ）----
Prm = parameters ;
fc  = Prm.Fc ;                                    % = 30 Hz
```

**マーカー前処理を関数化しておく**（9.4・9.5・9.8bで使い回す）。
`m3_analyze_single_trial.m` のNaN補間→ローパスと同じ手順。

```matlab
% ---- 1試行分のマーカーをフィルタして返すローカル関数（スクリプト末尾に配置）----
% ※ MATLABのスクリプト内ローカル関数として定義する（R2016b以降）
function M = preprocess_markers(Data, fc)
    fs = Data.FrameRate ;
    [b, a] = butter(2, fc/(fs/2)) ;

    % NaN補間（filtfiltの前に必須）
    fields = fieldnames(Data.Markers) ;
    for i = 1:numel(fields)
        x = Data.Markers.(fields{i}) ;
        t = (1:size(x,1))' ;
        for col = 1:size(x,2)
            nanIdx = isnan(x(:,col)) ;
            if any(nanIdx) && any(~nanIdx)
                x(nanIdx,col) = interp1(t(~nanIdx), x(~nanIdx,col), t(nanIdx), 'linear', 'extrap') ;
            end
        end
        Data.Markers.(fields{i}) = x ;
    end
    M = filt_all_fields(b, a, Data.Markers) ;
end
```

---

## 2. STEP A：フォースプレート系の新規指標（9.1〜9.3・9.7）

後方GRF・合成GRF・力積・RFD・体重移動タイミング・CoPを、全条件全試行で計算して
テーブルにまとめる。

```matlab
% ---- 結果を貯める（行=試行, 列=条件）----
PeakFyBack2 = nan(nTrial, nCondition) ;   % 踏み込み足の後方GRF [BW]
PeakFres2   = nan(nTrial, nCondition) ;   % 踏み込み足の合成GRF [BW]
ImpulseFz2  = nan(nTrial, nCondition) ;   % 踏み込み足Fzの力積 [BW·s]
PeakRFD2    = nan(nTrial, nCondition) ;   % 踏み込み足FzのRFD [BW/s]
TWeightXfer = nan(nTrial, nCondition) ;   % 体重移動タイミング [ms]
CoPpath2    = nan(nTrial, nCondition) ;   % 踏み込み足CoP総軌跡長

for iCondition = 1:nCondition
    for iTrial = 1:nTrial

        Data   = DataArray(iTrial, iCondition) ;
        Result = SingleTrialResultArray(iTrial, iCondition) ;

        % ---- ガード（x7_3 STEP3と同じ）----
        if ~isfield(Data, 'Force1') || isempty(Data.Force1), continue, end
        if isnan(Result.TCueMarker), continue, end

        fsAnalog   = Data.AnalogFs ;
        tCueAnalog = round(Result.TCueMarker / Data.FrameRate * fsAnalog) ;
        if tCueAnalog < 2, continue, end

        Fz1 = Data.Force1(:, 3) ;   Fz2 = Data.Force2(:, 3) ;
        Fy2 = Data.Force2(:, 2) ;

        bw = mean(Fz1(1:tCueAnalog-1)) + mean(Fz2(1:tCueAnalog-1)) ;   % [N]
        if bw <= 0, continue, end

        swingEnd   = min(tCueAnalog + round(2*fsAnalog), length(Fz1)) ;
        swingRange = tCueAnalog : swingEnd ;

        % ===== 9.1 後方GRF・合成GRF =====
        PeakFyBack2(iTrial, iCondition) = max(-Fy2(swingRange)) / bw ;
        Fres2 = sqrt(sum(Data.Force2.^2, 2)) ;
        PeakFres2(iTrial, iCondition)   = max(Fres2(swingRange)) / bw ;

        % ===== 9.2 力積・RFD（微分前にローパス）=====
        [bF, aF] = butter(2, 50/(fsAnalog/2)) ;
        Fz2_filt = filtfilt(bF, aF, Fz2) ;
        Fz2_static = mean(Fz2(1:tCueAnalog-1)) ;
        dFz2 = Fz2_filt(swingRange) - Fz2_static ;
        ImpulseFz2(iTrial, iCondition) = trapz(dFz2) / fsAnalog / bw ;
        dFz2dt = diff3p(Fz2_filt, 1/fsAnalog) ;
        PeakRFD2(iTrial, iCondition)   = max(dFz2dt(swingRange)) / bw ;

        % ===== 9.3 体重移動タイミング（50msデバウンス付き交差検出）=====
        minHold = round(0.05 * fsAnalog) ;
        isFront = Fz2(swingRange) > Fz1(swingRange) ;
        tCross  = NaN ;
        idx     = find(isFront, 1, 'first') ;
        while ~isempty(idx)
            holdEnd = min(idx + minHold - 1, length(isFront)) ;
            if all(isFront(idx:holdEnd))
                tCross = idx ; break
            end
            nextIdx = find(isFront(holdEnd+1:end), 1, 'first') ;
            if isempty(nextIdx), break, end
            idx = holdEnd + nextIdx ;
        end
        if ~isnan(tCross)
            TWeightXfer(iTrial, iCondition) = (swingRange(tCross) - tCueAnalog) / fsAnalog * 1000 ;
        end

        % ===== 9.7 CoP軌跡（踏み込み足=プレート2）=====
        Mx2 = Data.Moment2(:, 1) ;   My2 = Data.Moment2(:, 2) ;
        valid = Fz2 > 0.05 * bw ;
        copX = nan(size(Fz2)) ;   copY = nan(size(Fz2)) ;
        copX(valid) = -My2(valid) ./ Fz2(valid) ;
        copY(valid) =  Mx2(valid) ./ Fz2(valid) ;
        copXw = copX(swingRange) ;   copYw = copY(swingRange) ;
        CoPpath2(iTrial, iCondition) = sum(sqrt(diff(copXw).^2 + diff(copYw).^2), 'omitnan') ;

    end
end

% ---- 条件別平均を表示して妥当性を確認 ----
fprintf('\n=== Subject %02d フォースプレート系指標（条件別平均）===\n', iSubject) ;
fprintf('%-10s %8s %8s %8s %8s %10s\n', '条件', 'FyBack', 'Fres', 'Impulse', 'RFD', 'WtXfer[ms]') ;
for iCondition = 1:nCondition
    fprintf('%-10s %8.3f %8.3f %8.4f %8.1f %10.1f\n', ...
        ConditionNameArray{iCondition}, ...
        mean(PeakFyBack2(:,iCondition), 'omitnan'), ...
        mean(PeakFres2(:,iCondition),   'omitnan'), ...
        mean(ImpulseFz2(:,iCondition),  'omitnan'), ...
        mean(PeakRFD2(:,iCondition),    'omitnan'), ...
        mean(TWeightXfer(:,iCondition), 'omitnan')) ;
end
```

**確認ポイント**：

- [ ] 合成GRF（Fres）がピークFzより**小さくならない**（合成は各成分以上になるはず）
- [ ] 後方GRFが極端に大きく／負になっていないか（→ Fyの符号定義を疑う。9.9注意事項の②）
- [ ] 体重移動タイミングが概ね正の値（LED後に移動）か、自由スイングで負になっていないか

### 2-viz. STEP A の可視化

**(A-1) 5指標の条件別箱ひげ図**（大きさの条件比較）。共通のローカル関数
`boxplot_by_condition`（セクション8）を使う。

```matlab
figure('Name', 'STEP A: フォースプレート系指標') ; clf
subplot(2,3,1) ; boxplot_by_condition(PeakFyBack2, ConditionNameArray, 'Peak backward GRF [BW]') ; title('後方GRF（9.1）')
subplot(2,3,2) ; boxplot_by_condition(PeakFres2,   ConditionNameArray, 'Peak resultant GRF [BW]') ; title('合成GRF（9.1）')
subplot(2,3,3) ; boxplot_by_condition(ImpulseFz2,  ConditionNameArray, 'Impulse Fz [BW\cdots]') ; title('力積（9.2）')
subplot(2,3,4) ; boxplot_by_condition(PeakRFD2,    ConditionNameArray, 'Peak RFD [BW/s]') ; title('RFD（9.2）')
subplot(2,3,5) ; boxplot_by_condition(TWeightXfer, ConditionNameArray, 'Weight transfer [ms]') ; title('体重移動タイミング（9.3）')
sgtitle(sprintf('Subject %02d：フォースプレート系指標の条件比較', iSubject)) ;
```

**(A-2) 代表試行の時系列波形**（時間的な出来事の確認）。体重移動の交差線、力積の面積、
後方/合成GRFを1枚にまとめる。

```matlab
iCond_show  = 3 ;   % 見たい条件（1=free,2=simple,3=gonogo）
iTrial_show = 1 ;   % 見たい試行

Data   = DataArray(iTrial_show, iCond_show) ;
Result = SingleTrialResultArray(iTrial_show, iCond_show) ;

fsA  = Data.AnalogFs ;
tCue = round(Result.TCueMarker / Data.FrameRate * fsA) ;
Fz1  = Data.Force1(:,3) ;  Fz2 = Data.Force2(:,3) ;  Fy2 = Data.Force2(:,2) ;
bw   = mean(Fz1(1:tCue-1)) + mean(Fz2(1:tCue-1)) ;
Fres2 = sqrt(sum(Data.Force2.^2, 2)) ;
tA   = ((1:length(Fz1))' - tCue) / fsA ;                 % LED基準の時間軸[s]（列ベクトル）
swingEnd = min(tCue + round(2*fsA), length(Fz1)) ; swingRange = tCue:swingEnd ;

figure('Name', 'STEP A: 代表波形') ; clf

% 上段：体重移動（Fz1/Fz2 と交差線）
subplot(3,1,1)
plot(tA, Fz1/bw, 'b-', 'DisplayName', 'プレート1（軸足）') ; hold on
plot(tA, Fz2/bw, 'r-', 'DisplayName', 'プレート2（踏み込み足）') ;
xline(0, 'k--', 'LED') ;
if ~isnan(TWeightXfer(iTrial_show, iCond_show))
    xline(TWeightXfer(iTrial_show, iCond_show)/1000, 'm-', '体重移動') ;
end
xlim([-1 2]) ; ylabel('Fz [BW]') ; legend('Location','best') ; grid on
title('体重移動タイミング（9.3）：Fz1とFz2の交差')

% 中段：力積の面積とRFDの勾配
subplot(3,1,2)
[bF,aF] = butter(2, 50/(fsA/2)) ; Fz2f = filtfilt(bF, aF, Fz2) ;
dFz2 = Fz2f - mean(Fz2(1:tCue-1)) ;                       % 静止分を引いた増加分
area(tA(swingRange), dFz2(swingRange)/bw, 'FaceAlpha', 0.3) ; hold on
plot(tA, dFz2/bw, 'r-') ; xline(0, 'k--') ;
xlim([-1 2]) ; ylabel('\DeltaFz [BW]') ; grid on
title('力積＝塗り面積 / RFD＝立ち上がりの勾配（9.2）')

% 下段：後方GRF・合成GRF
subplot(3,1,3)
plot(tA, -Fy2/bw, 'g-', 'DisplayName', '後方GRF（-Fy）') ; hold on
plot(tA, Fres2/bw, 'k-', 'DisplayName', '合成GRF') ;
xline(0, 'k--') ; xlim([-1 2]) ; ylabel('[BW]') ; xlabel('LEDからの時間 [s]') ;
legend('Location','best') ; grid on
title('後方GRF・合成GRF（9.1）')

sgtitle(sprintf('Subject %02d  %s  Trial %d：代表波形', ...
    iSubject, ConditionNameArray{iCond_show}, iTrial_show)) ;
```

**(A-3) CoP軌跡（XY平面、時間で色分け）**。踏み込み足（プレート2）のCoPを空間的に見る。

```matlab
Mx2 = Data.Moment2(:,1) ;  My2 = Data.Moment2(:,2) ;
valid = Fz2 > 0.05*bw ;
copX = nan(size(Fz2)) ;  copY = nan(size(Fz2)) ;
copX(valid) = -My2(valid) ./ Fz2(valid) ;
copY(valid) =  Mx2(valid) ./ Fz2(valid) ;

figure('Name', 'STEP A: CoP軌跡') ; clf
plot(copX(swingRange), copY(swingRange), '-', 'Color', [0.6 0.6 0.6]) ; hold on
scatter(copX(swingRange), copY(swingRange), 18, tA(swingRange), 'filled') ;
cb = colorbar ; cb.Label.String = 'LEDからの時間 [s]' ;
axis equal ; grid on ; xlabel('CoP X') ; ylabel('CoP Y') ;
title(sprintf('Subject %02d  %s  Trial %d：CoP軌跡（9.7、色=時間経過）', ...
    iSubject, ConditionNameArray{iCond_show}, iTrial_show)) ;
```

**(A-4) 条件別スパゲッティ＋平均プロット（(A-2)代表波形の母集団版）**

(A-2)は「1試行だけ」を見る図だったため、その試行がたまたま特殊な動き（外れ値）だと
誤解を招く。ここでは**条件ごとに全試行を薄い線で重ね描きし、平均波形を太い線で上から重ねる**
ことで、「試行間のばらつきの大きさ」と「条件の典型的な傾向」を同時に見られるようにする。
(A-2)の3段（体重移動・力積・後方/合成GRF）を母集団バージョンとして描き直す。

> (A-2)（単一試行）はそのまま残しておくと、「特定の1試行だけ挙動がおかしい」ときの
> 詳細確認に使える。(A-4)は「条件として見たときの典型像」を確認する目的なので、両方使い分ける。

汎用的に使えるよう、「全試行薄線＋平均太線」の描画部分を**独立関数
`plot_trials_with_mean.m`** に切り出す（`boxplot_by_condition.m` と同じ考え方）。

```matlab
% ---- plot_trials_with_mean.m（新規ファイルとして作成）----
% 時間波形（横軸=時間、縦軸=値）用
function plot_trials_with_mean(tWin, WaveMat, lineColor)
% plot_trials_with_mean  全試行を薄く、平均波形を濃く重ねてプロットする
%   tWin      : 共通の時間軸 [1 x nTimePoints]（LED基準など）
%   WaveMat   : [nTrial x nTimePoints] 各試行の波形（NaNを含んでよい）
%   lineColor : [r g b]（0〜1）ベースとなる色。平均線はこの色そのもの、
%               個別試行はこの色を薄く（透明度を下げて）使う。

hold on

% ---- 全試行を薄く ----
% Line オブジェクトの Color に4番目の要素（アルファ値）を後から設定すると
% 透明度を付けられる（多くのMATLABバージョンで動作する一般的な方法）。
hLines = plot(tWin, WaveMat', 'LineWidth', 0.5) ;
for h = hLines'
    h.Color = [lineColor, 0.15] ;
end

% ---- 平均波形を濃く ----
meanWave = mean(WaveMat, 1, 'omitnan') ;
plot(tWin, meanWave, 'Color', lineColor, 'LineWidth', 2.5) ;

end
```

呼び出し側（`x7_4_visualize_grf_new.m` に追記するコード）。1回の試行ループで
必要な指標（体重移動用のFz1・Fz2、力積用のΔFz、後方GRF・合成GRF）をまとめて計算し、
3段×条件数のグリッドに描く。

```matlab
% ---- 各試行の波形をLED基準の共通時間軸に補間してから重ね描きする ----
% 試行ごとに録画長がわずかに違う可能性があるため、共通の時間軸(tWin)へ
% interp1で補間して揃える（範囲外はNaNにする＝伸ばして水増ししない）。

fsA_ref = DataArray(1,1).AnalogFs ;                 % 代表的なfsAnalog（全試行共通の想定。要確認）
tWin    = -1 : 1/fsA_ref : 2 ;                       % 共通の相対時間軸 [s]（LED基準）
nWin    = length(tWin) ;

figure('Name', 'STEP A: 条件別波形（薄=全試行, 濃=平均）') ; clf

for iCondition = 1:nCondition

    WaveFz1    = nan(nTrial, nWin) ;   % 軸足Fz
    WaveFz2    = nan(nTrial, nWin) ;   % 踏み込み足Fz
    WavedFz    = nan(nTrial, nWin) ;   % 力積の元波形（静止分を引いた増加分、フィルタ後）
    WaveFyBack = nan(nTrial, nWin) ;   % 後方GRF
    WaveFres   = nan(nTrial, nWin) ;   % 合成GRF

    for iTrial = 1:nTrial
        Data   = DataArray(iTrial, iCondition) ;
        Result = SingleTrialResultArray(iTrial, iCondition) ;

        if ~isfield(Data, 'Force1') || isempty(Data.Force1), continue, end
        if isnan(Result.TCueMarker), continue, end

        fsA  = Data.AnalogFs ;
        tCue = round(Result.TCueMarker / Data.FrameRate * fsA) ;
        if tCue < 2, continue, end

        Fz1 = Data.Force1(:, 3) ;   Fz2 = Data.Force2(:, 3) ;   Fy2 = Data.Force2(:, 2) ;
        if any(isnan(Fz1)) || any(isnan(Fz2)) || any(isnan(Fy2)), continue, end   % NaN試行は除外

        bw = mean(Fz1(1:tCue-1)) + mean(Fz2(1:tCue-1)) ;
        if bw <= 0, continue, end

        tTrial = ((1:length(Fz1))' - tCue) / fsA ;             % その試行のLED基準時間軸

        % ---- 体重移動（Fz1・Fz2）----
        WaveFz1(iTrial,:) = interp1(tTrial, Fz1/bw, tWin, 'linear', NaN) ;
        WaveFz2(iTrial,:) = interp1(tTrial, Fz2/bw, tWin, 'linear', NaN) ;

        % ---- 力積の元波形（フィルタ後、静止分を引く）----
        [bF, aF] = butter(2, 50/(fsA/2)) ;
        Fz2_filt = filtfilt(bF, aF, Fz2) ;
        dFz2 = (Fz2_filt - mean(Fz2(1:tCue-1))) / bw ;
        WavedFz(iTrial,:) = interp1(tTrial, dFz2, tWin, 'linear', NaN) ;

        % ---- 後方GRF・合成GRF ----
        Fres2 = sqrt(sum(Data.Force2.^2, 2)) ;
        WaveFyBack(iTrial,:) = interp1(tTrial, -Fy2/bw,  tWin, 'linear', NaN) ;
        WaveFres(iTrial,:)   = interp1(tTrial, Fres2/bw, tWin, 'linear', NaN) ;
    end

    nValid = sum(any(~isnan(WaveFz2), 2)) ;

    % --- 上段：体重移動 ---
    subplot(3, nCondition, iCondition)
    plot_trials_with_mean(tWin, WaveFz1, [0.2 0.4 0.9]) ;    % 軸足（青系）
    plot_trials_with_mean(tWin, WaveFz2, [0.9 0.3 0.2]) ;    % 踏み込み足（赤系）
    xline(0, 'k--') ; xlim([-1 2]) ; ylim([-0.2 2]) ; grid on
    ylabel('Fz [BW]') ; title(sprintf('%s（n=%d）体重移動', ConditionNameArray{iCondition}, nValid)) ;
    if iCondition == 1
        h1 = plot(nan,nan,'Color',[0.2 0.4 0.9],'LineWidth',2.5) ;
        h2 = plot(nan,nan,'Color',[0.9 0.3 0.2],'LineWidth',2.5) ;
        legend([h1 h2], {'軸足(平均)','踏み込み足(平均)'}, 'Location','best') ;
    end

    % --- 中段：力積の元波形 ---
    subplot(3, nCondition, nCondition + iCondition)
    plot_trials_with_mean(tWin, WavedFz, [0.6 0.2 0.6]) ;    % 紫系
    xline(0, 'k--') ; xlim([-1 2]) ; grid on
    ylabel('\DeltaFz [BW]') ; title('力積の元波形（9.2）')

    % --- 下段：後方GRF・合成GRF ---
    subplot(3, nCondition, 2*nCondition + iCondition)
    plot_trials_with_mean(tWin, WaveFyBack, [0.2 0.7 0.3]) ; % 緑系
    plot_trials_with_mean(tWin, WaveFres,   [0.1 0.1 0.1]) ; % 黒系
    xline(0, 'k--') ; xlim([-1 2]) ; grid on
    xlabel('LEDからの時間 [s]') ; ylabel('[BW]') ; title('後方/合成GRF（9.1）')
    if iCondition == 1
        h3 = plot(nan,nan,'Color',[0.2 0.7 0.3],'LineWidth',2.5) ;
        h4 = plot(nan,nan,'Color',[0.1 0.1 0.1],'LineWidth',2.5) ;
        legend([h3 h4], {'後方GRF(平均)','合成GRF(平均)'}, 'Location','best') ;
    end

end

sgtitle(sprintf('Subject %02d：条件別波形（薄=全試行, 濃=平均）', iSubject)) ;
```

**確認ポイント**：

- [ ] 各subplotのタイトルの `n=` が、その条件のNaN除外後の有効試行数として妥当か（20試行中、極端に少なくなっていないか）
- [ ] 平均線（太線）が、薄い試行線の束の中心付近を通っているか（明らかに外れていたら補間・整列のミスを疑う）
- [ ] 条件間で平均波形の交差タイミングやピークの高さに違いが見えるか

**ヘッド速度（STEP B）への応用**：`plot_trials_with_mean` は「時間軸 tWin」
「[nTrial × nTimePoints] の行列」「色」の3つを渡すだけの汎用関数なので、STEP Bの
ヘッド速度波形（`speed_top`、9.4参照）にも同じパターンをそのまま適用できる。

> **注意（未検証の前提）**：`fsA_ref`（共通の時間軸を作るためのサンプリング周波数）は
> 全試行で同一という前提を置いている。実際に試行ごとに`Data.AnalogFs`が変わらないか、
> 一度 `unique([DataArray.AnalogFs])` 等で確認しておくと安全。

---

**(A-5) 条件別CoP軌跡（(A-3)CoP軌跡の母集団版、条件ごとに別Figure）**

(A-4)と同じ「薄=全試行、濃=平均」の考え方をCoPだけに絞って適用する。CoPはXY平面上の
空間的な軌跡なので、時間波形用の`plot_trials_with_mean`とは別に、**XY軌跡専用の
独立関数`plot_trials_with_mean_xy.m`**を用意する。また、**条件ごとに別々のFigure
ウィンドウ**に分けて表示する（1つのFigure内でsubplotに並べるのではなく、
`figure('Name', ...)`を条件の数だけ呼ぶ）。

```matlab
% ---- plot_trials_with_mean_xy.m（新規ファイルとして作成）----
% XY軌跡（CoPなど、横軸・縦軸ともに空間座標）用
function plot_trials_with_mean_xy(XMat, YMat, lineColor)
% plot_trials_with_mean_xy  全試行のXY軌跡を薄く、平均軌跡を濃く重ねてプロットする
%   XMat, YMat : [nTrial x nTimePoints]（共通の時間軸に整列済み、NaN可）
%   lineColor  : [r g b]（0〜1）

hold on

hLines = plot(XMat', YMat', 'LineWidth', 0.5) ;
for h = hLines'
    h.Color = [lineColor, 0.15] ;
end

meanX = mean(XMat, 1, 'omitnan') ;
meanY = mean(YMat, 1, 'omitnan') ;
plot(meanX, meanY, 'Color', lineColor, 'LineWidth', 2.5) ;

end
```

呼び出し側（`x7_4_visualize_grf_new.m` に追記するコード）。CoP計算に必要な値だけを
計算する、独立した単体ループ（(A-4)の波形計算とは別に、これ単独で動く）。

```matlab
% ---- CoP軌跡：条件ごとに全試行を薄く＋平均を濃く、Figureは条件ごとに分ける ----

fsA_ref = DataArray(1,1).AnalogFs ;                 % 代表的なfsAnalog（全試行共通の想定。要確認）
tWin    = -1 : 1/fsA_ref : 2 ;                       % 共通の相対時間軸 [s]（LED基準）
nWin    = length(tWin) ;

for iCondition = 1:nCondition

    CoPXMat = nan(nTrial, nWin) ;   % CoP X
    CoPYMat = nan(nTrial, nWin) ;   % CoP Y

    for iTrial = 1:nTrial
        Data   = DataArray(iTrial, iCondition) ;
        Result = SingleTrialResultArray(iTrial, iCondition) ;

        if ~isfield(Data, 'Force1') || isempty(Data.Force1), continue, end
        if isnan(Result.TCueMarker), continue, end

        fsA  = Data.AnalogFs ;
        tCue = round(Result.TCueMarker / Data.FrameRate * fsA) ;
        if tCue < 2, continue, end

        Fz1 = Data.Force1(:, 3) ;   Fz2 = Data.Force2(:, 3) ;
        if any(isnan(Fz1)) || any(isnan(Fz2)), continue, end   % NaN試行は除外

        bw = mean(Fz1(1:tCue-1)) + mean(Fz2(1:tCue-1)) ;
        if bw <= 0, continue, end

        tTrial = ((1:length(Fz1))' - tCue) / fsA ;             % その試行のLED基準時間軸

        % ---- CoP（踏み込み足）----
        Mx2 = Data.Moment2(:,1) ;  My2 = Data.Moment2(:,2) ;
        validCoP = Fz2 > 0.05*bw ;
        copX = nan(size(Fz2)) ;  copY = nan(size(Fz2)) ;
        copX(validCoP) = -My2(validCoP) ./ Fz2(validCoP) ;
        copY(validCoP) =  Mx2(validCoP) ./ Fz2(validCoP) ;

        CoPXMat(iTrial,:) = interp1(tTrial, copX, tWin, 'linear', NaN) ;
        CoPYMat(iTrial,:) = interp1(tTrial, copY, tWin, 'linear', NaN) ;
    end

    nValid = sum(any(~isnan(CoPXMat), 2)) ;

    % ---- 条件ごとに別のFigureウィンドウを開く ----
    figure('Name', sprintf('CoP軌跡: %s', ConditionNameArray{iCondition})) ; clf
    plot_trials_with_mean_xy(CoPXMat, CoPYMat, [0.9 0.5 0.1]) ;   % 橙系
    axis equal ; grid on
    xlabel('CoP X') ; ylabel('CoP Y')
    title(sprintf('Subject %02d：%s（n=%d）CoP軌跡（薄=全試行, 濃=平均）', ...
        iSubject, ConditionNameArray{iCondition}, nValid)) ;

end
```

**確認ポイント**：

- [ ] 条件の数だけFigureウィンドウが開くこと（`free`・`simple`・`gonogo`で3枚）
- [ ] 各タイトルの `n=` が有効試行数として妥当か
- [ ] 平均軌跡（太線）が、薄い個別軌跡群からかけ離れた形になっていないか
  - 時間で対応点を平均しているため、試行間で動きのタイミングがバラバラだと
    平均軌跡が滑らかすぎる／縮んだ形になることがある。その場合は平均軌跡は
    あくまで参考程度に見て、個々の薄い軌跡のばらつきの方を重視する

> **注意（未検証の前提）**：`fsA_ref`は全試行で同一という前提を置いている（(A-4)と同じ）。

---

## 3. STEP B：バット系の新規指標（9.4 ヘッド線速度・9.5 MT）

マーカー前処理→`top` マーカーの並進速度→ピーク速度到達時刻→MTを計算する。

```matlab
PeakHeadSpeed = nan(nTrial, nCondition) ;   % [m/s]
MovementTime  = nan(nTrial, nCondition) ;   % [ms]

for iCondition = 1:nCondition
    for iTrial = 1:nTrial

        Data   = DataArray(iTrial, iCondition) ;
        Result = SingleTrialResultArray(iTrial, iCondition) ;

        if isnan(Result.TCueMarker), continue, end
        if ~isfield(Data.Markers, 'top'), continue, end

        fs = Data.FrameRate ;
        M  = preprocess_markers(Data, fc) ;

        % ---- 9.4 ヘッド線速度 ----
        v_top     = diff3p(M.top, 1/fs) ;
        speed_top = sqrt(sum(v_top.^2, 2)) / 1000 ;   % mm/s → m/s（単位は要確認）

        searchEnd   = min(Result.TCueMarker + 2*fs, length(speed_top)) ;
        searchRange = Result.TCueMarker : searchEnd ;
        [peakSpeed, iLocal] = max(speed_top(searchRange)) ;
        tPeakSpeed = searchRange(1) + iLocal - 1 ;

        PeakHeadSpeed(iTrial, iCondition) = peakSpeed ;

        % ---- 9.5 運動時間 MT ----
        if ~isnan(Result.SwingOnset)
            MovementTime(iTrial, iCondition) = (tPeakSpeed - Result.SwingOnset) / fs * 1000 ;
        end

    end
end

fprintf('\n=== Subject %02d バット系指標（条件別平均）===\n', iSubject) ;
fprintf('%-10s %12s %10s\n', '条件', 'HeadSpeed[m/s]', 'MT[ms]') ;
for iCondition = 1:nCondition
    fprintf('%-10s %12.2f %10.1f\n', ...
        ConditionNameArray{iCondition}, ...
        mean(PeakHeadSpeed(:,iCondition), 'omitnan'), ...
        mean(MovementTime(:,iCondition),  'omitnan')) ;
end
```

**確認ポイント**：

- [ ] ヘッド線速度が現実的な値（木製バットのヘッドスピードは概ね **20〜35 m/s** 程度）か
  - 桁がおかしい場合は単位（mm/m）を疑う（9.9注意事項の①）
- [ ] MTが正の値（数十〜百数十ms程度）か。負なら `SwingOnset` とピーク到達の前後関係を確認

### 3-viz. STEP B の可視化

**(B-1) 条件別箱ひげ図**（ヘッド線速度・MT）。

```matlab
figure('Name', 'STEP B: バット系指標') ; clf
subplot(1,2,1) ; boxplot_by_condition(PeakHeadSpeed, ConditionNameArray, 'Peak head speed [m/s]') ; title('ヘッド線速度（9.4）')
subplot(1,2,2) ; boxplot_by_condition(MovementTime,  ConditionNameArray, 'Movement time [ms]') ; title('運動時間 MT（9.5）')
sgtitle(sprintf('Subject %02d：バット系指標の条件比較', iSubject)) ;
```

**(B-2) 代表試行のヘッド速度波形**。LED・スイング開始・ピーク到達を線／マーカーで示し、
RT（LED→onset）とMT（onset→peak）の区間が目で追えるようにする。

```matlab
iCond_show  = 2 ;   iTrial_show = 1 ;

Data   = DataArray(iTrial_show, iCond_show) ;
Result = SingleTrialResultArray(iTrial_show, iCond_show) ;

fs    = Data.FrameRate ;
M     = preprocess_markers(Data, fc) ;
speed = sqrt(sum(diff3p(M.top, 1/fs).^2, 2)) / 1000 ;    % [m/s]
tM    = ((1:length(speed))' - Result.TCueMarker) / fs ;  % LED基準[s]

searchEnd = min(Result.TCueMarker + 2*fs, length(speed)) ; sr = Result.TCueMarker:searchEnd ;
[pk, iL]  = max(speed(sr)) ;  tPk = sr(1) + iL - 1 ;

figure('Name', 'STEP B: 速度波形') ; clf
plot(tM, speed, 'b-', 'LineWidth', 1.2) ; hold on
xline(0, 'k--', 'LED') ;
if ~isnan(Result.SwingOnset)
    xline((Result.SwingOnset - Result.TCueMarker)/fs, 'g-', 'Swing onset') ;
end
plot((tPk - Result.TCueMarker)/fs, pk, 'r*', 'MarkerSize', 12) ;
text((tPk - Result.TCueMarker)/fs, pk, '  peak') ;
xlim([-0.5 1.5]) ; xlabel('LEDからの時間 [s]') ; ylabel('Head speed [m/s]') ; grid on
title(sprintf('Subject %02d  %s  Trial %d：ヘッド速度（RT=%.0f ms → onset、onset→peak = MT）', ...
    iSubject, ConditionNameArray{iCond_show}, iTrial_show, Result.RT)) ;
```

---

## 4. STEP C：試行間変動性（9.6）

既存の `SingleTrialResultArray` からRT・ピーク角速度のSD/CVを条件別に出す。

```matlab
fprintf('\n=== Subject %02d 試行間変動性 ===\n', iSubject) ;
fprintf('%-10s %10s %8s %14s\n', '条件', 'RT_SD[ms]', 'RT_CV', 'Omega_CV') ;
for iCondition = 1:nCondition
    rtAll    = [SingleTrialResultArray(:, iCondition).RT] ;
    omegaAll = [SingleTrialResultArray(:, iCondition).PeakOmegaDeg] ;
    rtOK    = rtAll(~isnan(rtAll)) ;
    omegaOK = omegaAll(~isnan(omegaAll)) ;

    rt_sd    = std(rtOK) ;
    rt_cv    = std(rtOK) / mean(rtOK) ;
    omega_cv = std(omegaOK) / mean(omegaOK) ;

    fprintf('%-10s %10.1f %8.3f %14.3f\n', ...
        ConditionNameArray{iCondition}, rt_sd, rt_cv, omega_cv) ;
end
```

**確認ポイント**：

- [ ] free条件（RTを競わない）でRTのSDが他条件より大きい傾向が出るか（予想通りか）

### 4-viz. STEP C の可視化

変動性は「ばらつきの大きさ」そのものを見たいので、試行番号 vs 値の**散布図**が適している
（箱ひげ図より各試行の散らばりが直接見える）。

```matlab
figure('Name', 'STEP C: 試行間変動性') ; clf
colors = lines(nCondition) ;

subplot(1,2,1) ; hold on
for c = 1:nCondition
    rt = [SingleTrialResultArray(:, c).RT] ;
    scatter(1:nTrial, rt, 40, colors(c,:), 'filled', 'DisplayName', ConditionNameArray{c}) ;
end
xlabel('試行番号') ; ylabel('RT [ms]') ; legend('Location','best') ; grid on
title('RTの試行推移')

subplot(1,2,2) ; hold on
for c = 1:nCondition
    om = [SingleTrialResultArray(:, c).PeakOmegaDeg] ;
    scatter(1:nTrial, om, 40, colors(c,:), 'filled', 'DisplayName', ConditionNameArray{c}) ;
end
xlabel('試行番号') ; ylabel('Peak \omega [deg/s]') ; legend('Location','best') ; grid on
title('ピーク角速度の試行推移')

sgtitle(sprintf('Subject %02d：試行間変動性（点の縦の散らばり＝ばらつき）', iSubject)) ;
```

---

## 5. STEP D：抑制課題の指標（9.8、gonogoのNogo試行のみ）

予備実験にgostopは無いため、**gonogo条件のNogo試行**でエラー率とpost-error slowingの
コードだけを動作確認する。Nogoが5試行/被験者と少ないため参考値。

```matlab
iCondition = 3 ;   % gonogo

% ---- (a) エラー率（Nogoでスイングしてしまった率）----
nNogo = 0 ;   nNogoError = 0 ;
for iTrial = 1:nTrial
    R = SingleTrialResultArray(iTrial, iCondition) ;
    if strcmp(R.CueText, 'NoGo')
        nNogo = nNogo + 1 ;
        if ~isnan(R.SwingOnset), nNogoError = nNogoError + 1 ; end
    end
end
fprintf('\n=== Subject %02d 抑制課題（gonogo）===\n', iSubject) ;
if nNogo > 0
    fprintf('Nogo試行数=%d, commission error=%d, エラー率=%.2f\n', ...
        nNogo, nNogoError, nNogoError/nNogo) ;
else
    fprintf('Nogo試行が検出されませんでした（CueText/LED極性を確認）\n') ;
end

% ---- (c) post-error slowing（エラー直後のGo RT − 正答直後のGo RT）----
rtAfterError = [] ;   rtAfterCorrect = [] ;
for iTrial = 2:nTrial
    Rprev = SingleTrialResultArray(iTrial-1, iCondition) ;
    Rcurr = SingleTrialResultArray(iTrial,   iCondition) ;
    if ~strcmp(Rcurr.CueText, 'Go') || isnan(Rcurr.RT), continue, end
    if strcmp(Rprev.CueText, 'NoGo') && ~isnan(Rprev.SwingOnset)
        rtAfterError = [rtAfterError, Rcurr.RT] ;
    else
        rtAfterCorrect = [rtAfterCorrect, Rcurr.RT] ;
    end
end
if ~isempty(rtAfterError)
    fprintf('post-error slowing=%.1f ms（error後%d試行/正答後%d試行）\n', ...
        mean(rtAfterError)-mean(rtAfterCorrect), numel(rtAfterError), numel(rtAfterCorrect)) ;
else
    fprintf('エラー直後のGo試行が無く、post-error slowingは算出不可（試行数の制約）\n') ;
end
```

> **9.8(b) 停止時最大バット角度**は、`4.7_チェックスイング判定基準` の角度定義（左右打者・
> 座標系の符号）を確定してから追加するのが安全なため、本検証コードでは省略した。
> バット角度の計算自体は `M.top`・`M.bottom` があれば予備実験データでも実行できる。

### 5-viz. STEP D の可視化

内訳や対比を見るので**棒グラフ**が適している。

```matlab
figure('Name', 'STEP D: 抑制課題') ; clf

% 左：Nogo試行の内訳（エラー vs 抑制成功）
subplot(1,2,1)
bar([nNogoError, nNogo - nNogoError]) ;
set(gca, 'XTickLabel', {'error(スイング)', '正答(抑制成功)'}) ;
ylabel('試行数') ; grid on
title(sprintf('Nogo内訳（エラー率=%.2f）', nNogoError / max(nNogo,1))) ;

% 右：post-error slowing（正答後 vs エラー後のGo RT）
subplot(1,2,2)
if ~isempty(rtAfterError)
    bar([mean(rtAfterCorrect), mean(rtAfterError)]) ;
    set(gca, 'XTickLabel', {'正答後のGo', 'error後のGo'}) ;
    ylabel('RT [ms]') ; grid on
    title(sprintf('post-error slowing = %.1f ms', mean(rtAfterError) - mean(rtAfterCorrect))) ;
else
    title('error後のGo試行なし（試行数不足）') ;
end

sgtitle(sprintf('Subject %02d：抑制課題（gonogo）', iSubject)) ;
```

> Nogo試行が5試行/被験者と少ないため、これらの図は**コードの動作確認と傾向の一瞥**が目的。
> 統計的な結論には使わない。

---

## 6. STEP E：全被験者まとめ（任意）

STEP A〜Dは被験者1名（`iSubject`）を対象にしている。S01・S02を同じ図に重ねたい場合は、
各指標の格納行列（`PeakHeadSpeed` など）を被験者ループの外に用意して被験者ごとに列を
足していけばよい。予備実験は2名のみのため、まずは1名ずつ図を確認する運用で十分。

---

## 7. 検証の進め方（推奨）

1. **まずSTEP A・B を S01 で実行**し、出力値の桁・符号が妥当かを9.9注意事項表と照合する
   （特にヘッド線速度の単位、後方GRF/Fyの符号）。
2. 妥当性が取れたら `iSubject = 2` でも実行し、被験者間で破綻しないか確認する。
3. STEP C・D は参考値として眺める（統計的結論は出さない）。
4. 値が安定して取れることを確認できたら、本実験用（4条件・10試行）へは
   `ConditionNameArray` を4条件に戻し、`技術説明.md` セクション9の形で
   `m3_analyze_single_trial.m` へフィールド追加 → x4〜x6再実行、の流れに乗せる。

> **注意**：本コードは**未実行・未検証の提案**である。実行して値が想定と違う場合は、
> まず9.9注意事項表（単位・符号・モーメント基準点）を確認し、原因を切り分けること。

---

## 8. ローカル関数の定義（スクリプト末尾に配置する）

セクション1の `preprocess_markers` と、可視化で使う `boxplot_by_condition` は、
MATLABのローカル関数として**1つのスクリプトファイルの末尾にまとめて置く**（R2016b以降）。

```matlab
% ---- マーカー前処理（NaN補間→ローパス、m3と同じ手順）----
function M = preprocess_markers(Data, fc)
    fs = Data.FrameRate ;
    [b, a] = butter(2, fc/(fs/2)) ;

    fields = fieldnames(Data.Markers) ;
    for i = 1:numel(fields)
        x = Data.Markers.(fields{i}) ;
        t = (1:size(x,1))' ;
        for col = 1:size(x,2)
            nanIdx = isnan(x(:,col)) ;
            if any(nanIdx) && any(~nanIdx)
                x(nanIdx,col) = interp1(t(~nanIdx), x(~nanIdx,col), t(nanIdx), 'linear', 'extrap') ;
            end
        end
        Data.Markers.(fields{i}) = x ;
    end
    M = filt_all_fields(b, a, Data.Markers) ;
end

% ---- 条件別の箱ひげ図（NaNを除外して条件ごとに並べる）----
function boxplot_by_condition(dataMat, condNames, ylab)
    vals = [] ;   grp = {} ;
    for c = 1:numel(condNames)
        v = dataMat(~isnan(dataMat(:,c)), c) ;
        vals = [vals ; v] ;
        grp  = [grp ; repmat(condNames(c), numel(v), 1)] ;
    end
    if ~isempty(vals)
        boxplot(vals, grp, 'GroupOrder', condNames) ;
    end
    ylabel(ylab) ; grid on
end
```

> **配置上の注意**：MATLABでは、スクリプト内でローカル関数を使う場合、**関数定義はすべて
> ファイルの最後**に置く必要がある（実行文より後）。STEP A〜Dのコードを1つの `.m` ファイルに
> まとめる場合、この2関数を末尾に貼り付けること。セクション1に再掲した
> `preprocess_markers` はここと同一物なので、重複して定義しないよう1つにまとめる。
