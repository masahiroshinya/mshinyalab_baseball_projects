# 本実験用LED基盤 設計および作成マニュアル（AO直接駆動・逆並列回路版）

## 概要
本設計は、現状のMATLABコードのロジックをそのまま活かし、NI DAQのアナログ出力（AO）2チャネルの**プラス・マイナス電圧**を利用して3色のLEDを制御する、シンプルかつ堅牢な設計です。トランジスタや外部電源を必要としない「逆並列（Anti-parallel）回路」を採用しています。
被験者（打者）から見やすい提示用ボックス（設備）を作成することを目的とします。

## 物理レイアウト（配置案）
被験者から見て、LEDは以下のように**横一列**に配置し、黒いケースに組み込むことで視認性を高めます。
- **左側**：赤色LED（No-Go / ボール）
- **中央**：白色LED（Ready / 投球モーション開始）
- **右側**：緑色LED（Go / ストライク）
※ 中央の白を注視点（投手のリリースポイント）とします。左右に配置することで、周辺視野でも空間的な区別がつきやすくなります。

## 回路設計の考え方（逆並列回路）
LEDは一方向にしか電流を流さない特性（ダイオード特性）を持ちます。これを利用し、同じチャネルに2つのLEDを逆向きに接続することで、出力する電圧のプラス/マイナスで光る色を切り替えます。

- **【AO0チャネル】 トリガー ＆ 白色LED**
  - **+5V出力時**：Qualisysの計測開始トリガー信号として利用します。DAQのAO0端子に「BNC T型分岐コネクタ」を取り付け、片方をQualisysへ、もう片方をLED基盤へ接続します（LED側は電流の向きが逆になるため白LEDは光りません）。
  - **-5V出力時**：白色LEDが点灯します（電流経路：GND → 白LED → AO0）。

- **【AO1チャネル】 緑色LED ＆ 赤色LED**
  - **+5V出力時**：緑色LEDが点灯します（電流経路：AO1 → 緑LED → GND）。
  - **-5V出力時**：赤色LEDが点灯します（電流経路：GND → 赤LED → AO1）。

## 必要物品（買い物リスト）

| 部品カテゴリ | 品名・スペック | 数量 | 備考 |
| --- | --- | --- | --- |
| **LED** | 超高輝度LED（白・赤・緑） | 各色1個 | DAQの微弱な電流（数mA）でも明るく光る「超高輝度」タイプを強く推奨。5mm〜10mmサイズ。 |
| **抵抗** | 330Ω 〜 1kΩ | 3個 | 各LEDに直列に繋ぐ電流制限用。DAQの出力ピン保護のため必須です。 |
| **配線ケーブル** | BNCケーブル（オス - オス） | 2本 | DAQのAO0, AO1端子からマウンド（LEDボックス）までの3m延長用。市販のBNCケーブルで確実な通信が可能です。 |
| **コネクタ類** | BNC T型分岐コネクタ（オス - メス×2）| 1個 | DAQのAO0端子に取り付け、Qualisysトリガー用とLED基盤用の信号を物理的に分岐させます。 |
| **コネクタ類** | BNCジャック（メス・基板用/パネル用）| 2個 | LEDボックス側に取り付け、3mのBNCケーブルをカチッと接続できるようにします。 |
| **基板** | ユニバーサル基板（穴あき基板） | 1枚 | LEDと抵抗をはんだ付けする土台。 |
| **外装** | 黒色プラスチックケース | 1個 | 背景を黒くするため。側面にBNCジャックを取り付ける穴を開けて使います。 |
| **アクセサリ** | 光拡散キャップ | 3個 | 高輝度LEDの直進的な光を拡散させ、どの角度からでも見やすくするため（推奨）。 |

## 回路図（結線図）

```mermaid
graph TD
    BNC_AO0[LED側 BNCジャック AO0]
    BNC_AO1[LED側 BNCジャック AO1]
    GND[GND: BNC外側のシールド同士を接続]

    BNC_AO0 -.->|シールド| GND
    BNC_AO1 -.->|シールド| GND

    subgraph 中央_白色LED回路
        GND -->|電流| LED_White((白LED アノード))
        LED_White -->|カソード| R_W[抵抗 330Ω]
        R_W --> AO0
        %% 注：AO0が-5Vの時のみ電流が流れる
    end

    subgraph 右側_緑色LED回路
        AO1 -->|電流| R_G[抵抗 330Ω]
        R_G --> LED_Green((緑LED アノード))
        LED_Green -->|カソード| GND
        %% 注：AO1が+5Vの時のみ電流が流れる
    end

    subgraph 左側_赤色LED回路
        GND -->|電流| LED_Red((赤LED アノード))
        LED_Red -->|カソード| R_R[抵抗 330Ω]
        R_R --> AO1
        %% 注：AO1が-5Vの時のみ電流が流れる
    end
```

## 実装時の注意点と工夫

1. **LEDの極性（プラス・マイナス）に細心の注意を**
   LEDには極性があります。足が長い方（アノード）が電流の「入り口」、短い方（カソード）が「出口」です。回路図に従って正しくはんだ付けしてください。**特に白と赤は、GND側がアノードになる特殊な繋ぎ方（逆接続）です。**
2. **DAQの出力電流制限について**
   NI DAQのアナログ出力は、通常数mA程度の電流しか流せません。そのため、必ず「超高輝度タイプ」のLEDを使用してください（少ない電流でも十分な明るさを得られます）。抵抗値は330Ωから始め、もし明るさが足りなければDAQの仕様（最大出力電流）の範囲内で抵抗値を下げて調整してください。
3. **被験者の見やすさ（生態学的妥当性）の確保**
   LEDをケースに組み込む際は、実際の投球軌道や投手のリリースポイントを想定し、打者が自然な視線で構えられる高さや位置に設置できるよう、ケース底面に三脚マウント用のネジ穴（1/4インチネジ）を接着するなどの工夫を推奨します。
4. **3mの配線延長に関する懸念（電圧降下・ノイズ）について**
   - **電圧降下**：3m程度のBNCケーブル（同軸ケーブル）の抵抗値は極めて低いため、LEDの光量が落ちる心配はありません。
   - **ノイズ・遅延**：BNCケーブルはノイズに強いシールド構造になっているため、実験室環境での3m延長は極めて安定しています。反応時間の計測に影響する遅延も全く発生しません。安心して配線を伸ばしてください。
