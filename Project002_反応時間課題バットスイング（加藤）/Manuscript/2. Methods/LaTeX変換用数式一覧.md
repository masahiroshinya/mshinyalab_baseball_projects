# LaTeX変換用数式一覧（draft.md 対応）

`draft.md`「2-X. 分析」に登場する数式・数式記号を、Word の数式エディタ（LaTeX入力モード）に
そのまま貼り付けられる形でまとめたもの。

**Wordでの使い方**：`Alt`+`=` で数式ボックスを挿入 → 数式タブの入力形式を「LaTeX」に切り替え →
下記の文字列を数式ボックスに貼り付ける。

---

## 1. ディスプレイ数式（独立した式として表示）

### 1-1. 中心差分（速度ベクトルの算出）― draft.md 146〜148行目付近

```
\mathbf{v}(t_i) = \frac{\mathbf{p}(t_{i+1}) - \mathbf{p}(t_{i-1})}{2\,\Delta t}
```

### 1-2. 合成速度（速度ベクトルのノルム）― draft.md 154〜156行目付近

```
|\mathbf{v}(t)| = \sqrt{v_x(t)^2 + v_y(t)^2 + v_z(t)^2}
```

---

## 2. 文中のインライン数式

| 記号                         | 意味                       | LaTeX 文字列                          | 出現箇所（draft.md） |
| :--------------------------- | :------------------------- | :------------------------------------- | :-------------------- |
| 位置ベクトル **p**(t)   | topマーカーの位置座標     | `\mathbf{p}(t) = (x(t), y(t), z(t))` | 144行目                |
| 速度ベクトル **v**(t)   | 中心差分で算出した速度     | `\mathbf{v}(t)`                      | 144・150行目           |
| サンプリング間隔 Δt         | 250 Hz のサンプリング間隔 | `\Delta t`                           | 144行目                |
| Δt の具体値                 | 1/250 秒                  | `\Delta t = 1/250\ \text{s}`         | 144行目                |
| 投手方向速度成分 v_x(t) | 速度ベクトルの X 成分      | `v_x(t)`                             | 158行目                |

---

## 更新履歴

- 2026-07-25：新規作成（draft.md の分析セクション整理に伴い、数式のLaTeX変換用文字列をまとめた）
