作業日：2026-09-10

ユーザー：加藤 和太郎（student）

セッションの概要：

**試行の除外基準を全面的に再設計し、下流の図・ドキュメントまで反映させたセッション。**
発端は「エラー試行はどう除外されているのか」という問いで、調べると除外が4層に分散し、
**必要な基準が無く、不要な基準が効いている**状態だった。

- **除外基準を2つに限定した**（`03_Analysis/技術説明.md` §10）
  - ① `top` マーカーの欠損（**解析窓内** = cue 後 0〜2 s に、補間で埋まらなかった欠損）
  - ② 床反力が正常に計測できていない（`Fz1`/`Fz2` またはピーク値の欠損）
  - `NoGo`・`Stop` の選別は課題設計上の選別として別軸で維持
- **撤廃した除外は5つ。** 全マーカー対象の `HasLongNan`（下流未参照）、`ErrorCode 99`（同）、
  `x4` の `try/catch` による試行ごとの切り捨て、`x6` の `isempty(NetVelTop)` による**行の削除**、
  記録全体の `any(isnan(Fz))`。`IsBadBW` は `BWest` ベースに差し替えた
- **設計原則を3つ立てた。** (1) 行は捨てずフラグで選別（不可逆な除外点をなくす）、
  (2) 指標ごとに前提条件を確かめ、成立しないものだけを NaN にする（§3.9 の教訓）、
  (3) 例外は除外基準ではなく直すべきバグとして扱う（`rethrow`）
- **MATLAB R2026a で x2〜x8 を実行して検証した。** `x4` の例外 0 件（§3.9 では 75 試行）、
  `x6` は 298 試行すべて保持、`IsBadGRF` による除外 0 件。
  **`PeakFz2_BW` は全 20 セルが満数・110.3〜150.6 %BW で Orishimo et al. (2023) と整合**し、
  §8 の【最優先】だった「S03 の %BW が 24% 過大」も解消した
- **判定基準は実測を見て2回変更した。** 当初案（`top` は記録全体、床反力は末端荷重の乖離）は
  実データで代償が大きすぎることが分かり、`s_count_excluded.m` と分布の比較を経て
  「解析窓内」「欠損のみ」に確定した（§10.8）
- **下流の 5.6 / 5.8 を新基準で作り直した。** 5.6 は `p0`/`p1` を新設、
  5.8 は `i0`〜`i3` を新設して旧 `h0`〜`h3` と旧 PNG を削除。
  **どちらも中央値はほとんど動かず、旧基準は筋が悪かったが結論を歪めてはいなかった**
- **5.8 では指標名の誤りをユーザーが発見。** `RT → Fzピーク` は「時間の長さ」から「時点」への
  矢印で意味をなさないので `Onset → Fzピーク` に改めた。あわせて gonogo の外れ値
  （`S02` 行13、1580 ms）の原因を特定し、位置による判定で除外した

備考（未解決課題など）：

**次にやること（優先順）**

1. **`x5` の目視確認（20回）。** `x2`〜`x4` は実行済みだが `x5` は対話が必要なため未実行。
   `x5_...Checked/` が旧構成のままなので、通さないと `x6` 以降が動かない。
   このとき **S03・S04 は `IsBadTop` で多くの試行が落ちる**ので、目視の印象と判定が
   一致しているかを確認しておくと §9.6 の QTM 再処理の判断材料になる
2. **`x7_check` を S03・S04 でも通す。** `x7_MultiTrialAnalysisResultsChecked/` に
   S03・S04 が無い。`x8` は無いものを**警告なしにスキップ**するので、忘れると
   3名分の統計テーブルが静かに出来上がる

**判断が必要な事項**

- **`top` マーカーの座標の飛びは、新しい2基準では捕まらない**（5.6 §10.3）。
  `S03 gonogo` 行6 が `PeakVel` 117.9 m/s（フレーム間変位 936 mm、正常は 138〜161 mm）で、
  窓内の欠損は 0 なので `IsBadTop` を素通りする。
  **`parameters.m:66` の `Prm.QC.TopStepMaxMm = 200` は定義済みだが未使用**で、
  §9.4 の実測（ジャンプ 216〜1250 mm / 正常 138〜161 mm）から見てそのまま使える。
  基準を1つ足すかは要判断
- **`PeakVelTop` は S03・S04 が使えない**（`n` が 1〜6）。判定範囲をピーク近傍まで
  緩めても S04 simple は 14/15 が不良のまま。**§9.6 の QTM 再処理が唯一の解決策**で、
  それが済むまで `PeakVelTop` を確定値として扱わないこと
- **`x7_3_visualize_grf.m` が独自の `isBadBW` を持っている**（68行目）。`x8` を差し替えた
  だけでは図と統計値が食い違う。`x8_StatTable` を読む形に作り直すのが望ましい（§8）
- **Methods 2-4 の改訂が必要。** 除外基準を2つに限定したこと、および %BW の分母を
  実測体重ではなく記録末端からの推定体重にしたことの両方が本文と食い違う
- **5.6 / 5.8 の PNG の `_新除外基準` という接尾辞が意味を失っている**（比較相手が無い）。
  外すなら `outPath` を直して再実行する
- **`S01 gonogo` のピーク鉛直GRF が未確認**（5.8 §6）。`i2` が唯一「要目視確認」に
  挙げる項目で、175.1 %BW の試行を目視すること

**記録した教訓**

- `try`/`catch` で試行ごと捨てる作りは、失敗した指標と無関係な指標まで巻き添えにする
- **ある検出結果を別の判定の前提に使うときは、その用途に足る精度かを別に確かめる。**
  接地時刻は RT の探索窓としては 50 ms の許容で足りていたが、
  「ピークが接地から離れているか」の基準点としては足りず、250 ms に分ける必要があった
- 値の範囲による事後フィルタは筋が悪いが、**結論を歪めているとは限らない**。
  置き換えたら必ず旧結果と突き合わせて、何が変わって何が変わらなかったかを記録する

**ロール制約の上書きについて**

`userinfo.md` のロールは student で、プロジェクト `CLAUDE.md` は「student の場合は
修正案の提示のみ、ファイルの更新を行わない」と定めている。この点を伝えたところ
ユーザーから「コードの変更を許可します」と明示的な許可があったため、`.m` の更新まで実施した。
教員が経緯を追えるよう、トピック2にも記録している。

---

## AIとの個別のやり取り

### 1. 03_Analysis におけるエラー試行の除外システムの調査

**指示・質問：**

Experiments/Main Experiments/03_Analysis におけるエラー試行の除外について、どのようなシステムで除外が行われているか教えてください。

**AIの対応：**

`03_Analysis/` 配下の .m ファイルと `技術説明.md` を横断的に調査し、除外の仕組みを整理して回答した。
参照したのは `parameters.m`（ErrorCode 定義）、`x2_import_data.m`（コード付与）、
`x4_analyze_single_trial.m`（NoData スキップと try/catch）、`x6_analyze_multi_trial.m`
（`isempty(NetVelTop)` でテーブルに入れない）、`x7_*_visualize_*.m`（`ExcludedCueTextArray`）、
`x8_make_stat_table.m`（`IsBadBW` / `IsGo` フラグ）、および技術説明 §3.9・§4・§7.4・§8。

**決定事項・結果：**

- 除外は単一の関数ではなく、**4層（x2 のエラーコード付与 → x4 の空結果化 → x6 のテーブル除外 → x7/x8 のフラグ選別）**に分散していることを確認した。
- 基本方針は「上流では捨てず、下流でフラグ選別」（技術説明 §4・§7.4）。ただし **x6 の `isempty(Result.NetVelTop)` 判定だけは行そのものを落とす**ため、ここが実質的な不可逆の除外点であることを指摘した。
- **課題遂行上の誤反応（NoGo/Stop でスイングした、Go でスイングしなかった）を判定・除外する仕組みは現状どこにも存在しない**ことを指摘した（`x7_check_multi_trial_results.m` のコメント「NoGo の抑制判定はこのスクリプトでは行わない（別途実施）」）。除外されているのは「キュー種別による設計上の除外」と「計測不良・解析失敗による除外」のみ。
- ファイル変更なし（調査・回答のみ）。

---

### 2. 除外基準の再設計と反映

**指示・質問：**

現状の分析過程では、topマーカーと床反力のみを必要としています。また、LED信号が欠落している試行はありません。topマーカーに連続してNaNがある試行や、床反力が正常に計測できていない試行は除外したいですが、それ以外の除外基準はなくしたいです。

（続けて）反映を実施してください。 / コードの変更を許可します

**AIの対応：**

3点を確認したうえで（① NoGo・Stop はフラグとして残す、② top の連続NaN は記録全体で判定、
③ 床反力は記録末端の推定体重を基準に判定）、修正案を提示。

**★ ロール制約の上書き：** `userinfo.md` のロールは student であり、プロジェクト `CLAUDE.md` は
「student の場合は修正案の提示のみ、ファイルの更新を行わない」と定めている。この点を伝えたところ、
ユーザーから「コードの変更を許可します」と明示的な許可があったため、`.m` の更新まで実施した。
教員が経緯を追えるよう、ここに記録しておく。

**決定事項・結果：**

- **仕様を技術説明 §10 として新設**（[技術説明.md](../Experiments/Main experiments/03_Analysis/技術説明.md)）。
  決定内容・再設計の理由・設計原則・変更箇所・適用手順・留保事項を記載。
- **除外基準を2つに限定した。** ① `top` の最長連続NaN > 10 フレーム（記録全体）、
  ② 床反力が正常に計測できていない（`BWTail` が推定体重から ±20% 超ずれる、または欠損）。
  `NoGo`・`Stop` の選別（`IsGo`）は課題設計上の選別として別軸で維持。
- **撤廃した除外：** 全マーカー対象の `HasLongNan`（下流未参照）、`ErrorCode 99`（同）、
  `x4` の `try`/`catch` による試行ごとの切り捨て（`rethrow` に変更）、
  `x6` の `isempty(NetVelTop)` による行の削除（`IsNoData` 判定に変更）、
  記録全体の `any(isnan(Fz))`（末尾NaN切り落とし＋窓内判定に変更）、`IsBadBW`（`BWest` ベースに差し替え）。
- **新設した仕組み：** `Prm.Excl.*`、`x2` の `maxNanRun()`、`m3` の `BWTail` 出力と
  `top` 欠落ガード、`x8` の `IsBadGRF` / `BWest_N`、診断スクリプト `s_count_excluded.m`。
- **副次的に解消した既知課題：** 技術説明 §8 の【最優先】「床反力の正規化の分母を記録末端から
  推定した体重に差し替える」を実装した（S03 の %BW が 24% 過大になる問題）。
  §7.9 で失われていた gonogo/gostop Trial 5 も、末尾NaNの切り落としで復活する見込み。
- **変更ファイル：** `parameters.m` / `x2_import_data.m` / `m3_analyze_single_trial.m` /
  `x4_analyze_single_trial.m` / `x5_check_single_trial_results.m` / `x6_analyze_multi_trial.m` /
  `x8_make_stat_table.m` / `s_count_excluded.m`（新規） / `技術説明.md`
- **未実行。** この環境に MATLAB が無いため動作確認していない。`x2` から流し直しが必要
  （`DataArray` と `Result` の両方にフィールドが増えるため）。
- **要判断として残した点：** 判定範囲「記録全体」は §9.4 の反例（S02 gonogo 13 は総欠損 175
  フレームだが窓内0で正常）と衝突する。`s_count_excluded.m` で残存試行数を確認してから確定する。

---

### 3. MATLAB による実行確認

**指示・質問：**

MATLABプラグインしているので一度確認してもらうことはできますか？

**AIの対応：**

`/Applications/MATLAB_R2026a.app/bin/matlab -batch` で実行。`-batch` に複数行の文字列を
直接渡すと引数が渡らないため、scratchpad に実行用 .m を書いて `run()` で呼ぶ形にした。

1. `checkcode` で変更8ファイルの構文チェック
2. `x2` → `x3` → `x4` を実データで実行
3. `s_count_excluded` で残存試行数を確認
4. `x6` → `x8` は目視確認済みフォルダ（x5_/x7_）を汚さないよう scratchpad にミラーを作成し、
   `x4` の出力を `x5_...Checked` 相当として実行
5. 判定範囲3案（記録全体／解析窓内／ピーク近傍）の比較と、末端荷重で落ちる試行の
   `PeakFz2` 分布の比較

**決定事項・結果：**

- **コードは正しく動いた。** 詳細は技術説明 §10.8。
  - `checkcode` エラー0、`x4` の例外 **0 件**（§3.9 では 75 試行）、`x6` は 298 試行すべて保持
  - S03 の推定体重 **593.2 N（60.5 kg）が `sandbox/5.7` の記録値と一致**
  - `PeakFz2_BW` が全被験者 111.7〜151.9 %BW に収まり、**S03 の 24% 過大が解消**
  - S01 gonogo の `n_PeakFz2_BW` が 8 → 9 に回復（§7.9）
- **★ ただし判定基準2点に問題が判明した（要判断）。**
  - **① `top` の判定範囲「記録全体」は代償が大きい。** §9.4 の警告どおり、S01 free・
    S01 gonogo・S02 gostop はピーク近傍に欠損がないのに落ちる。`n_PeakVelTop` が
    S02 gostop = 1、S04 simple = 1 まで低下。ピーク近傍判定なら S01・S02・S05 は全試行残る。
    S03・S04 はどの範囲でも救えず、QTM 再処理（§9.6）が必要。
  - **② 末端荷重による除外は的を外している。** 落とす試行の `PeakFz2` 分布は残す試行と
    ほぼ同一（中央値 130.1 vs 135.9 %BW）で、正常データを 53 本（21%）捨てている。
    末端で降りているかどうかはスイング計測の妥当性と無関係。`BWest` は分母専用にし、
    除外は「床反力に欠損がある」だけにするのが妥当。
- `s_count_excluded.m` は事前割り当てに修正（`checkcode` の指摘対応）。
- 中間ファイル x2_Data / x3_DataChecked / x4_SingleTrialAnalysisResults は
  新しいフィールド構成で再生成済み。**x5 の目視確認は未実施**（対話が必要なため）。

---

### 4. 判定基準の確定と再検証

**指示・質問：**

（AskUserQuestion への回答）top の判定範囲 = 解析窓内（cue後0〜2s）、
床反力 = 末端荷重による除外をやめて欠損だけで判定

**AIの対応：**

判定を `x2` から `m3` へ移し（cue 位置が必要なため）、`x8` の `IsBadGRF` から末端乖離の項を外した。
`s_count_excluded.m` は x4 の結果を読んで3案を並記する形に書き換え、
「ピークが解析窓の外にある試行」の数も表示するようにした。そのうえで x2〜x8 を再実行。

**決定事項・結果：**

- **除外基準の最終形**（技術説明 §10.1）
  - ① `top`：**解析窓内（cue 後 0〜2 s）に、補間で埋まらなかった欠損があるか** → `IsBadTop`
  - ② 床反力：**`Fz1`/`Fz2` またはピーク値が欠損しているか** → `IsBadGRF`
  - `BWest`（記録末端 0.5 s からの推定体重）は**除外には使わず %BW の分母としてのみ使う**
- **実装の要点：** `m3` は補間ループの**前**に `top` の欠損マスクを保存する。補間ループが
  NaN の長さに関係なく `linear`/`extrap` で全部埋めるため、後では補間の産物か判別できない。
  `IsBadTop` は `true` で初期化し、cue が確定してから窓を切って上書きする。
- **再検証の結果**（技術説明 §10.9）
  - `checkcode` エラー0、`x4` の例外0、`x6` は 298 試行すべて保持
  - **`IsBadGRF` による除外 0 件。** 床反力は全 20 セルが満数、110.3〜150.6 %BW で
    Orishimo et al. (2023) の 159 ± 29 %BW と整合。**統計に進める状態になった**
  - `IsBadTop` は Go 試行 247 本のうち 86 本
- **★ 残った問題：`PeakVelTop` は S03・S04 が使えない。** S04 simple は n=1、S03 gostop は n=3。
  判定範囲をピーク近傍まで緩めても S04 simple は 14/15 が不良のまま。除外基準の設計では
  解決できず、**§9.6 の QTM 再処理（S05 の設定を S03・S04 に適用して Relational fill）が必要**。
- **新たに可視化した食い違い：** `PeakVelTop` は `max(netVelTop)` で試行全体から取っており
  探索窓を持たない（§9.3）ので、窓内判定と範囲が食い違う。実測では 247 本中 1 本
  （S03 gostop）のみ。`s_count_excluded.m` が毎回この数を表示する。
- **未実施：** `x5` の目視確認（対話が必要）。これを通さないと `x6` 以降が動かない。

---

### 5. 5.6 の条件別図を新しい除外基準で描き直し

**指示・質問：**

sandbox/5.6_プロット/条件別_全被験者_RT-MT-PeakVel-Slope.png
これを新しい解析結果で分析し直して同フォルダ上に新たにpngファイルとして出力してください。

**AIの対応：**

旧図の生成スクリプトはフォルダに残っていなかったため（`untitled.m` は1試行用）、
5.7 の g0/g1・5.8 の h0/h1 と同じ「算出と作図を分ける」構成で新規作成した。
指標の定義は §4 の採用案そのままにし、除外基準だけを差し替えた。

**決定事項・結果：**

- **新規ファイル**（`sandbox/5.6_プロット/`）
  - `p0_calc_metrics.m` … 4指標の算出。判定①は `x4` の `Result.IsBadTop` をそのまま読む
    （5.6 側で再実装すると図と統計で違う試行が落ちるため）
  - `p1_plot_by_condition.m` … 作図と PNG 出力（h1 の作図コードを流用）
  - `条件別_全被験者_RT-MT-PeakVel-Slope_新除外基準.png` … 出力
- **除外基準の置き換え**：旧図の「値の範囲による事後フィルタ」（PeakVel 5〜40 m/s・
  MT>0・RT 100〜600 ms、258→217 試行）を廃止し、`03_Analysis` §10.1 の2基準に。
  Go 試行 247 本 → RT 225 / PeakVel 161 / MT・Slope 149。
  **指標ごとに前提条件を判定する**（①は PeakVel・MT・Slope、②は RT・MT・Slope）。
- **★ 中央値はほとんど動かなかった。16セル中15セルで差が 2 以内**、条件の順序もすべて保たれた。
  旧図の事後フィルタは筋の悪い基準だったが**結論を歪めてはいなかった**ことが確認できた。
  唯一動いたのは gostop の MT（+11.5 ms）で、n が 46→28 と最も減った偏りによる。
- **★ 判明した穴：座標の飛びは新基準では捕まらない。** S03 gonogo 行6 が
  PeakVel 117.9 m/s（フレーム間変位 936 mm、正常は 138〜161 mm）で、
  窓内の欠損は 0 なので `IsBadTop` を素通りする。§9.4 の「ジャンプと欠損は別問題」のとおり。
  **`parameters.m` の `Prm.QC.TopStepMaxMm = 200` は定義済みだが未使用**で、
  §9.4 の実測（ジャンプ 216〜1250 mm / 正常 138〜161 mm）から見てそのまま使える。
  基準を1つ足すかは要判断。
- 外れ値で軸が潰れたため、**四分位範囲の3倍で軸を切り、はみ出した点は橙の △▽ で境界に置いて
  数と実際の値を注記**する形にした。これは表示の都合であって除外ではない（図中にも明記）。
- 経緯は `sandbox/5.6_プロット/技術説明.md` §10 に記録。§9 の未解決課題のうち
  「ErrorCode 1 が 96% に立っている」は解決済みとして取り消し線を付けた（今回の起点になった指摘）。

---

### 6. 5.8 の Fz 立ち上がり分析を新しい除外基準でやり直し

**指示・質問：**

sandbox/5.8_Fzの分析もやり直して新たに保存してください。

**AIの対応：**

`h0`〜`h3` と旧 PNG を残したまま、新しい除外基準版を `i0`〜`i3` として追加。
`checkcode` でエラー0を確認したうえで i1・i2・i3 を実行して PNG 3枚を出力した。

**決定事項・結果：**

- **新規ファイル**（`sandbox/5.8_踏み込み足Fzの立ち上がり/`）
  - `i0_calc_rfd_metrics.m` / `i1_plot_rfd_by_condition.m` /
    `i2_plot_cv_by_condition.m` / `i3_plot_example_trials.m`
  - `条件別_全被験者_Fz立ち上がり_新除外基準.png` /
    `条件別_全被験者_変動係数_新除外基準.png` / `代表試行_波形確認_新除外基準.png`
- **★ 変えたのは実質1行。** `h0` は Go 試行のうち `BWBase`（cue 前の Fz1+Fz2）が
  被験者内中央値から ±20% ずれる試行を `continue` で落としていた。`i0` は
  `continue` をやめ、数だけ数える（`Diag.nBadBWRef`）。指標の定義は5つとも同一。
- **★ 本フォルダの5指標はすべて床反力ベースなので、新基準①（top の欠損）は無関係。**
  ②（床反力の欠損）だけが効き、それは `h0` の時点で `Trial.ok` として既に実装済みだった。
- **試行数：** 4指標すべて算出できた試行 214 → **225**。
  **ピーク鉛直GRF は全 20 セルが満数**（free 73・simple 75・gonogo 50・gostop 49 = 247、
  旧 236）。
- **★ 中央値は 20 セルすべてで差が 4 以内。** §3-4 の読み取り（ピーク値は free 最大・
  simple 最小、立ち上がり速度は逆に simple 最大）も §3-5 の分解もそのまま成り立つ。
- **★ RT の中央値 259.0/190.0/199.0/208.0 が 5.6 の `p0` と完全に一致した。**
  同じ定義を別スクリプトで独立に実装したものが一致したので、実装の取り違えがない裏付け。
- **★ 最大の収穫は S03 simple。** RT 系の n が **5 → 12**、CV が 56.2% → 48.0%。
  §3-6 の「n=5 なので CV の推定そのものが信用できない」という警告と、5.7 の
  「S03 の simple に真の計測不良が6件」という記述は、**どちらも `isBadBW` が
  作り出したものだった**（実体は S03 の構えの違い）。両方に取り消し線を入れた。
- **CV の中央値は 2 セルしか動かなかった**（ピーク鉛直GRF simple 6.8→6.7、
  正規化 RFD simple 10.9→11.5）。5名の中央値がちょうど S04 の値で、S04 には
  `BWBase` 逸脱の試行が無かったため。
- **`i3` の出力ガードを変更。** `h3` は毎回 `input` で y/n を尋ねる作りで `-batch` では
  落ちる。確認が守っているのは「既存の図を見ないまま差し替えること」なので、
  **ファイルが無いときは尋ねずに書き出す**形にした（上書き時は従来どおり尋ねる）。
- 経緯は `sandbox/5.8_踏み込み足Fzの立ち上がり/技術説明.md` §7 に記録。
- **申し送り：** `h*` と `i*` の両方が残っているので、どちらを本採用にするか決めたら
  使わない側を退避すること。両方あると論文に書いた数字がどちらか分からなくなる。

---

### 7. 5.8 の旧コード・旧出力を削除

**指示・質問：**

5.8フォルダ内の旧コードで出力したものについては削除してください。コードについても同様。

**AIの対応：**

削除前に git 追跡状況を確認し、未追跡（＝削除すると復元不可）の1点だけ控えを取ってから削除した。
そのうえで、削除によって参照先が無くなったドキュメントとコードの記述を整合させた。

**決定事項・結果：**

- **削除したもの**（`sandbox/5.8_踏み込み足Fzの立ち上がり/`）
  - コード4本：`h0_calc_rfd_metrics.m` / `h1_plot_rfd_by_condition.m` /
    `h2_plot_cv_by_condition.m` / `h3_plot_example_trials.m`
  - PNG 2枚：`条件別_全被験者_Fz立ち上がり.png` / `条件別_全被験者_変動係数.png`
  - PNG 1枚：`代表試行_波形確認.png`（**未追跡だった**ため scratchpad に控えを取った）
  - `個別グラフ/`（5枚）… 削除した `条件別_全被験者_Fz立ち上がり.png` を切り出したもので、
    元図が無くなったため。原稿からの参照は無いことを `Manuscript/` の grep で確認済み
- **復元性：** `代表試行_波形確認.png` 以外はすべて git 追跡下（コミット `3f67cb2` 以前）なので
  `git checkout` で戻せる。`h3` に未コミットの差分が1行あったが、内容は
  1行目の余分な `n` を消すだけの修正で、`i3` には既に取り込まれているため実質の損失なし
- **ドキュメントの整合（技術説明.md）**
  - §1 のファイル構成を現存ファイルのみに書き換え、**「名前の履歴」の注記を追加**
    （当初 `h0`〜`h3` → 新基準版 `i0`〜`i3` を並置 → 旧一式を削除。§2〜§6 は `h0` について
    書かれたものだが指標の定義は同一なのでそのまま通用する、という断り）
  - **§2-0 の「計測不良試行の除外（BWBase）」を「2026-09-10 に廃止」に書き換えた。**
    式は残し、なぜ撤回したか（落としていた試行の分布が残す試行と同一、247中53本）を明記
  - §4-4 の内訳表示を `i0` の出力（`BWBase 逸脱（★参考。除外せず）: 11` / 225 試行）に差し替え
  - §2〜§6 の `h0`〜`h3` 参照 17 件を `i0`〜`i3` に置換。§1 の履歴注記と §7 の
    「旧 `h0` との対比」は歴史的記述なので残した
  - §7-6 の申し送り「両方残してある」を取り消し線にし、代わりに
    「PNG の `_新除外基準` 接尾辞が意味を失っている」「`個別グラフ/` も削除した」を追加
- **コードの整合：** `i0`〜`i3` のヘッダ・本文にあった削除済みファイルへの参照
  （`h1_plot_rfd_by_condition.m が行う`、`h0 が設計した係数` など）を `i*` に修正。
  「h0 と完全に同一」等の履歴記述は残した
- **削除後の動作確認：** `checkcode` 4本すべてエラー0、`i1` を実行して
  Go 試行 247・4指標 225・PNG 出力まで通ることを確認
- **申し送り：** PNG の `_新除外基準` という接尾辞は比較相手が無くなったので意味を失っている。
  外すなら `i1`/`i2`/`i3` の `outPath` を直して再実行（3か所）。

---

### 8. 指標名の誤りの修正と、指標3 の外れ値の原因特定

**指示・質問：**

RT〜Fzピークまでの時間のグラフでgonogo条件においてひとつ外れ値が出ていると思います。
あと、正しいタイトルはOnset~Fzピーク値までの時間ではないでしょうか？確認してください。

**AIの対応：**

2点とも確認し、どちらもユーザーの指摘どおりだった。

**決定事項・結果：**

**① 指標名の誤り（ユーザーの指摘が正しい）**

- `RT` は $t_c \to t_\mathrm{on}$ の**時間の長さ**であって時点ではないので、
  「RT → Fzピーク」と書くと長さから時点への矢印になり意味をなさない。
  測っているのは $t_\mathrm{pk} - t_\mathrm{on}$ = **onset を起点とした時間**。
- **`i3_plot_example_trials.m` は当初からこれを `MT` と呼んでおり、そちらが正しかった。**
  この量は 5.6 の MT（onset → バット先端の最大速度）と同じ構造で、終点を Fz2 のピークに
  替えたものである。
- 修正：`i0` の `MetricName{3}` を `Onset → Fzピーク の時間 [ms]` に変更。
  あわせて `RT 時点の Fz2` → `Onset 時点の Fz2` など表記を統一
  （`i0` 9箇所・`i1` 4箇所・技術説明 16箇所）。数式の添字も
  $T_{\mathrm{RT}\to\mathrm{pk}}$ → $T_{\mathrm{on}\to\mathrm{pk}}$ に直した。
- 技術説明 §2-3 に改名の経緯と理由を注記。`i2` の CV パネル名は `MetricName` 由来なので自動追随。
- PNG 3枚を再出力。

**② gonogo の外れ値（S02 行13、1580 ms）の原因を特定 → 技術説明 §3-7 を新設**

- §3-6 で「後続動作を拾っている疑い」としていたものを波形で確認し、**疑いどおりだった。**

| 時刻（cue 基準） | 出来事 | Fz2 |
| --- | --- | --- |
| 0.226 s | onset | 約 0.4 %BW（空中） |
| 0.545 s | 接地 | 50 %BW |
| **0.662 s** | **スイングのピーク** | **147.9 %BW** |
| **1.806 s** | **後続動作のピーク（採用されている）** | **152.1 %BW** |

- **`max()` は正しく動いている。** 探索窓（cue → cue+2 s）内で後続動作の山が
  スイングのピークを **2.8% だけ**上回ったため選ばれた。
- **影響：** ピーク値（指標2）は 147.9 → 152.1 でほぼ無害だが、
  時間を分母に持つ指標3〜5 が壊れる（指標3: 436 → 1580 ms、
  指標4: 約 338 → 96.1 %BW/s）。§3-6 で S02 gonogo の3指標が同時に
  「要目視確認」に挙がったのはこのため。
- **同種は 225 試行中この1件だけ。** 全試行を「接地→接地+0.5 s の窓内の最大値が
  採用ピークの 90% 以上あり、かつ採用ピークが接地+0.5 s より後」で走査して確認。
  系統的な問題ではなく単発の事故。
- **対処は未適用（§6 の申し送りに残した）。** 二択:
  1. 探索窓を「接地 → 接地+0.5 s」に狭める → この試行は直るが、**指標2 の窓が
     変わって 5.7 の `g0` と照合できなくなり全試行の値が動く**
  2. この1試行を落とす → ただし**「値がおかしい試行を値で落とす」のは今回廃止した
     種類の基準**なので、採るなら「ピークが接地から 0.5 s 以上離れている」という
     **位置による基準**にすべき

---

### 9. 指標3 の外れ値試行を除外（位置による判定）

**指示・質問：**

この１試行だけ落としてグラフを出力して下さい。過去のものは消去してOK

**AIの対応：**

`i0` に「ピークが接地から離れすぎている試行は落とす」判定を追加し、PNG 3枚を上書き出力。
**「1580 ms は長すぎる」という値による判定にはしていない**（値で切ると今回廃止した種類の
基準に戻るため）。技術説明 §3-7 に対処と、途中でつまずいた点を追記。

**決定事項・結果：**

- **追加したパラメータ**（`i0_calc_rfd_metrics.m`）
  ```matlab
  PrmPk.MaxAfterFCSec = 0.5 ;   % 接地からこれ以上離れたピークは採らない
  PrmPk.MinFCSec      = 0.25 ;  % 接地がこれより早い試行は、接地が信用できない
  ```
- **★ つまずいた点：最初の実装で 13 試行が落ちた。** RT 検出のゲート
  （`PrmRT.MinWinSample` = 50 ms）をそのまま位置判定の前提に使ったため。
  内訳は目的の1件＋`nShortWin` 10件＋`nNoOnset` 2件で、後者12件は接地が
  cue から 50〜91 ms の「浮かせ残り」（§4-4）。**`tFC` 自体が偽なので
  「ピークが接地から離れている」の判定が意味をなさない**（ピークは 0.58〜0.59 s の正常位置）。
- **対処：位置判定用の接地の信頼性を、RT 検出用より厳しく分けた。**
  真の接地は実測で 351〜1116 ms（中央値 600 ms）なので 250 ms を境にすれば安全に分離できる。
  `isFCforRT`（50 ms、従来）と `isFCforPk`（250 ms、新規）の2段階にした。
  **教訓：ある検出結果を別の判定の前提に使うときは、その用途に足る精度かを別に確かめる。**
- **結果：除外はちょうど 1 試行（`S02 gonogo` 行13）。**
  - 指標2 の n: 247 → **246**、指標3〜5 の n: 225 → **224**
  - 探索窓（cue → cue+2 s）は変えていないので **5.7 の `g0` との照合はそのまま可能**
  - 条件の中央値はほとんど動かない（gonogo の `Onset → ピーク` 512.5 → 512.0 ms）
  - **`S02 gonogo` の CV が劇的に改善：`Onset → ピーク` 59.9% → 9.2%、
    RFD 26.7% → 14.5%、正規化 RFD 25.3% → 8.5%**
  - **`i2` が「要目視確認」に挙げる件数が 5 件 → 1 件**（残るのは
    `S01 gonogo` のピーク鉛直GRF、CV 13.9%、175.1 %BW の試行。§6 の申し送りに追加）
- **条件の中央値はほぼ不変なのに CV が 59.9% → 9.2% に落ちた。**
  1試行の外れ値が CV をどれだけ動かすかの実例（§3-6 ★の注意そのもの）。
- PNG 3枚を上書き出力。`i1` の脚注に位置判定の説明を3行追加（余白も調整）。

