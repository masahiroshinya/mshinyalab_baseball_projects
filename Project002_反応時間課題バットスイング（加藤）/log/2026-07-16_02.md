作業日：2026-07-16
ユーザー：加藤 和太郎（role: student）

セッションの概要：
予備実験（2026-0604）の解析を、シングルトライアル解析コード `m3_analyze_single_trial.m` の書き直しからやり直す作業に着手した。解析項目を ①反応時間 ②バット先端マーカー並進速度 ③床反力Fz の3つに決定し、作業ステップ1〜4を立てて、作業記録用MD `予備実験解析し直し/00_解析し直し方針と作業記録.md` を新規作成した（以降、判断とその根拠をすべてこのMDに蓄積している）。**ステップ1（並進速度の算出）は完了。** マーカー座標を mm→m に直してから `diff3p` で微分する実装を提案し、S01 free条件で PeakVelTop = 25.8〜27.3 m/s を得た。角速度（約1800 deg/s ≒ 31.4 rad/s）との突き合わせで r = 26/31.4 ≒ 0.83 m というバット長として妥当な値が出たことから、単位変換が正しいことを確認した。**ステップ2（閾値決め）では、S01 のデータから絶対閾値 1.0 m/s を決定した**が、**ステップ3（S02での検証）でこの閾値が完全に破綻した。** S02 はバットを常時揺らして構えるタイプの被験者で、キュー前ベースラインが最大 1.154 m/s（アンカー 1.0 を超える）、NoGo最大が 1.533 m/s に達し、**Go/NoGo を分離でき、かつ動作開始を検出できる閾値が存在しない**ことが判明した。閾値 0.3 での平均RTが free 143.4 ms と生理的下限（約150 ms）を下回ることが決定的な証拠。診断は「S02 では『静止→動き出し』という前提が成立せず、バット先端速度による動作開始の検出が原理的に不可能」。救済策として、NoGo判定を立ち上がり（紙一重の勝負）からピーク速度（1.5 vs 25 m/s、16倍のマージン）へ移す設計変更と、RT検出の信号そのものを ③床反力Fz へ移す方針転換を提案した。ユーザーは判断の前に、バットマーカーから反応時間を算出している先行研究を調査することとし、本セッションを終了した。studentロールに従い、解析コード本体（`m3_analyze_single_trial.m` 等）の更新は一切行っていない（提案のみ。作成・更新したのは作業記録MD・検討用スクリプトのリネーム・ログ）。

備考（未解決課題など）：
- **次回の再開地点：先行研究の調査結果を踏まえ、RT検出の信号をバット先端速度のままにするか、Fz に移すかを決定する。** 判断材料として `s2e_check_fz_baseline.m`（FzのベースラインSDとNoGo最大逸脱を両被験者で測る、コードはMDに記載済み）を実行する必要がある。
- **S02 の波形は一度も見られていない。** `s2a_check_velocity_waveform.m` を S02 で実行したがグラフが表示されず、貼られた数値（平均 0.122, SD 0.015, 最大 0.173）は S01 の古い出力だった（S02 simple のベースライン平均は s2b では 0.240）。エラーで止まっていないか要確認。
- 「スイングがあったか」の判定をピーク速度（例：10 m/s）で行う設計変更は、両被験者で成立するため採用予定。ただし未実装。
- **S02 gonogo Trial 1 が `LEDData(:,2)` でエラー**（「位置 2 のインデックスが配列範囲を超えています」）。ステップ1とは独立した既存の不具合。`size(DataArray(1,3).LEDData)` で切り分け、`技術説明.md` に記録すること。未対応。
- `Force1` / `Force2` のどちらが踏み込み足か未確認。`x7_4_visualize_grf_new.m` のコメントでは Force2 とされている。
- free条件の扱いは「RTは算出するが反応時間としては報告しない」と決定済み（free だけ RT が約400 ms 遅く、ベースラインも最も高いため、自己ペースで振っていると判断）。
- 案B-4（極小値による逆探索）の検証スクリプトはコマンドウィンドウで実行したためファイルが残っていない。棄却済みのため実害はないが再現不可。結果と理由はMDに記載済み。
- ステップ4（Fz の実装）は未着手。マーカーとアナログでサンプリング周波数が違う点が実装上の注意点。

---

## AIとの個別のやり取り

### 1. 解析し直しの方針決定

**指示・質問：**
> シングルタスクの解析コードを書き直す形で予備実験の分析をし直そうと思います。
> 解析項目は、①反応時間、②バット先端マーカー並進速度、③床反力Fzで行こうと思います

**AIの対応：**
- userinfo.md でユーザー（加藤 和太郎・student）を確認。既存の `m3_analyze_single_trial.m`・`x4_analyze_single_trial.m`・`parameters.m`・`diff3p.m`・`load_qualisys_mat.m`・`x7_3`/`x7_4`（GRF可視化の試作）を確認した。
- 現行コードでは「反応時間」と「バットの速度指標」が**同じ信号（角速度）から**出ている構造であることを指摘し、指標を並進速度に変えるにあたって、この2つを切り離すのか一本化するのかを決める必要があると提示。
- 選択肢を提示して確認を取った。

**決定事項・結果：**
- **①スイング開始の検出信号：バット先端の並進速度**（角速度300 deg/s による検出は廃止）
- **③Fzの出力：ピーク値（体重正規化）／立ち上がり時刻／波形そのもの** の3点
- 書き直し対象は `m3_analyze_single_trial.m`（本体）、`parameters.m`（閾値追加）、`x4_analyze_single_trial.m`（出力フィールドの辻褄合わせ）。

### 2. 方針の抜き出しと作業ステップの決定

**指示・質問：**
> 一気にやると作業量が多いので、一つずつやっていこうと思います。
> 方針だけ抜き出して、もう一度提示してください。

**AIの対応：**
- コードを外して方針のみを提示。作業ステップ1〜4（①並進速度の算出 → ②波形を見て閾値決め → ③RT実装 → ④Fz実装）を提案した。
- ステップ2 を「今回の山場」と位置づけ、並進速度は体重移動でもベースラインがゼロにならないため、閾値の決め方は波形を見てから判断すべきと説明。

**決定事項・結果：**
- 作業ステップ1〜4 を決定。①②を先に片付けてパイプライン（x4→x5→x6→x7）を一度通す方針。

### 3. 作業記録フォルダ・MDの作成とステップ1の実施

**指示・質問：**
> ステップ１から始めましょう。方針から「予備実験解析し直し」フォルダを作って、その中にMDファイルを作成して、そこに作業したことを記録しながら進めていきたい。

**AIの対応：**
- `Preliminary experiments/予備実験解析し直し/00_解析し直し方針と作業記録.md` を新規作成（背景・解析項目・対象ファイル・作業ステップ・未解決事項・作業記録の構成）。
  - ※ 後にユーザーがフォルダを `Preliminary experiments/2026-0604/MATLAB/予備実験解析し直し/` へ移動。
- ステップ1 の修正案を提示：角速度ブロックの前に並進速度の計算を追加する。
  ```matlab
  posTop    = M.top / 1000 ;                    % [m]
  velTop    = diff3p(posTop, 1/fs) ;            % [m/s] 中心差分
  netVelTop = sum(velTop.^2, 2).^0.5 ;          % [m/s] ノルム＝速さ
  Result.NetVelTop  = netVelTop ;
  Result.PeakVelTop = max(netVelTop) ;
  ```
- **設計判断の説明**：微分は線形操作なので mm→m を先に割っても後で割っても同じだが、先に割ると以降すべてが m/s に統一され単位事故を防げる（学習資料8.1で保留にしていた単位の扱いを確定）。
- `diff3p` の第2引数はサンプリング**間隔** h であり `fs` ではなく `1/fs` を渡すこと、`sum(...,2).^0.5` がノルム＝速さ（スカラー）であることを、学習資料の3章・2章と対応づけて説明。
- 角速度は RT 検出がまだ依存しているため暫定的に残す方針を提示（ステップ3で削除）。
- `x4_analyze_single_trial.m` の `makeEmptyResult()` に `NetVelTop`・`PeakVelTop` を追加する必要性を説明（m3 の出力と食い違うと構造体配列の代入でエラーになるため）。

**決定事項・結果：**
- 新規作成：`予備実験解析し直し/00_解析し直し方針と作業記録.md`。
- studentロールに従い、コードは提案のみ（ユーザーが適用）。

### 4. ステップ1 の実行結果の確認と検証

**指示・質問：**
> 通りました。（x4 の実行結果を貼付：全被験者・全条件のRT一覧）
> 上記が結果です。

**AIの対応：**
- **この出力ではステップ1の成果（②）が確認できていない**ことを指摘。表示されているRTは角速度ベースのままで、変わっていないのが正しい状態と説明。
- `PeakVelTop` の値を確認するコードを提示。見るポイントは**桁**であり、野球のスイングなら 15〜30 m/s になるはずと提示。
- あわせて、出力から気づいた3点を報告：
  1. **NoGo試行でRTが出ている**（S01 Trial 9 で 7020 ms、S02 で 4728・4936・5220 ms）。原因は探索範囲が試行終了までの全区間になっていること。並進速度に切り替えると偽陽性はむしろ増える方向。
  2. **free条件のRTが異常に長い**（S01で880〜1524 ms、simpleは544〜840 ms）。free が自己ペースなら「RT」は指標として意味を持たない。
  3. **S02 gonogo Trial 1 がエラー**（`Data.LEDData(:, 2)` で2列目が存在しない形）。既存の不具合。

**決定事項・結果：**
- 説明・確認依頼のみ。ファイル変更なし。

### 5. PeakVelTop の検証（ステップ1 完了）

**指示・質問：**
> （PeakVelTop と PeakOmegaDeg の一覧を貼付）
> 結果です、全試行25m/s近辺なので、妥当かと思います。

**AIの対応：**
- **角速度との突き合わせによる検証**を提示。PeakOmegaDeg ≈ 1800 deg/s = 31.4 rad/s、v = ω × r より r ≈ 26/31.4 ≒ **0.83 m** となり、バット長として妥当。mm のまま微分していれば r = 830 m という物理的にありえない値になったはずで、**単位変換が正しいことの裏付け**になると説明。
- 全20試行が 25.8〜27.3 m/s（ばらつき約2%）に収まっており、NaN補間やフィルタが波形を壊していないことも示唆すると補足。
- 作業記録MDにステップ1を完了として記録（変更内容、設計判断の理由、コードの要点、検証結果、発見した3つの問題）。

**決定事項・結果：**
- **ステップ1 完了。** `00_解析し直し方針と作業記録.md` を更新。

### 6. ステップ2 の開始（波形の目視）

**AIの対応：**
- 波形確認スクリプトを提示。**figure 2 の縦軸を 0〜3 m/s に絞る**のが肝であり、ピークが26 m/sあるため全体表示では立ち上がりの根元が見えないと説明。

**指示・質問：**
> コマンドウインドウにおく形でもいいですか＞

**AIの対応：**
- 動くが**ファイルを勧める**と回答。理由は(1)ステップ2は条件や縮尺を変えて何度も回す作業であること、(2)`pause` が後続の貼り付けテキストをキー入力として食う罠があること。
- **前言を訂正**：置き場所として提案した `予備実験解析し直し/` は誤りで、プロジェクトに既に `s4_SingleTrialAnalysisScratch/`（1試行内で完結する分析の検討用フォルダ）が存在することを指摘。同フォルダのREADMEに「（分析区間などは、プロットして生データを見てから決める必要がある）」とあり、ステップ2〜4はこのフォルダの想定内の作業だと説明。

**決定事項・結果：**
- ユーザーは `予備実験解析し直し/`（MATLABフォルダ配下へ移動済み）にスクリプトを配置した。

### 7. 波形の観察結果とベースライン統計

**指示・質問：**
> （S01 simple Trial 1 の拡大波形の画像）これが結果です。
> Trial  1: ベースライン（キュー前0.5s） 平均 0.122, SD 0.015, 最大 0.173 m/s
> ベースライン値はこんな感じ

**AIの対応：**
- 波形から3点を読み取り：
  1. **中間の盛り上がりがない**（→ 後にステップ3で撤回）
  2. **角速度による検出（赤破線 t≈0.66 s）は実際の動き出し（t≈0.40 s）より約260 ms遅い**。従来のRTは「スイングが十分加速した時刻」を測っていた。
  3. **立ち上がりが急峻**（0.4sで0.15 m/s → 0.65sで3 m/s）。
- **相対閾値（平均＋k×SD）が使えない**ことを指摘：SDが小さすぎ（0.015）、平均＋5SD = 0.197 でベースライン最大 0.173 とほぼ差がない。Fzと違い並進速度のベースラインは「ノイズ」ではなく「実際の微動」なのでSD基準が機能しない。
- 1試行だけで決めるのは危険として、確認1（全試行のベースライン統計）・確認2（閾値感度分析）・確認3（NoGo試行の最大速度）を一度に回すスクリプトを提示。

**決定事項・結果：**
- 説明・確認依頼のみ。

### 8. 閾値の決定（ステップ2 完了）

**指示・質問：**
> （確認1〜3 の実行結果を貼付：ベースライン最大の最大 free 0.441 / NoGo全体の最大 0.589 / 閾値感度分析）

**AIの対応：**
- **0.5 m/s を棄却**。NoGo最大 0.589 を下回るため Trial 5・13 を偽陽性で拾う。「0.5で余裕がある」という前回の判断は Trial 1 だけを見た**間違い**だったと明示的に訂正。
- **0.3 m/s も棄却**。ベースライン最大 0.441 を下回る。free の感度分析で 0.3→0.5 が 391 ms 飛んでいるのは、スイングではなくベースラインの雑音を検出しているため。
- **1.0 m/s を提案**。NoGo最大の1.7倍・ベースライン最大の2.3倍のマージン。0.75 は余裕が1.27倍しかなく危険。0.75→1.0 のRT差は +24 ms と小さく、2.0 まで上げても検出できた試行数は 20/20・16/16 のままなのでコストが低い。
- **単一閾値の原理的な弱点**（閾値を上げるほど偽陽性は減るが検出が遅れ、RTが系統的に過大評価される）を指摘し、**案B（アンカー＋逆探索）**を提示。
- **free条件について証拠が揃った**と報告：閾値0.5での平均RTが free 940 / simple 538 / gonogo 521 ms で free だけ400 ms遅い。ベースラインも最も高い。「free条件では被験者がキューに反応しておらず、自分のタイミングで振っている」ことを強く示唆する。
- MDに、確認1〜3の全数値、各候補を棄却した理由の表、「1試行だけで閾値を決めてはいけない」という反省を記録。

**決定事項・結果：**
- **閾値 1.0 m/s（絶対閾値）に決定。** `00_解析し直し方針と作業記録.md` を更新。

### 9. 検出アルゴリズムと free条件の扱いの決定

**AIの対応：**
- 選択肢を提示して確認を取った（案A：単一閾値／案B：逆探索、および free の RT の扱い）。

**指示・質問：**
> 相対決定でピーク速度の3%を超えた瞬間、とかではどうでしょうか？
（free条件については「算出はするが反応時間としては報告しない」を選択）

**AIの対応：**
- 発想は筋が良く、到達運動の研究では「ピーク速度の5%」が古典的な運動開始基準であると認めたうえで、**単独では使えない致命的な穴**を指摘：
  - **NoGo試行にはピークが存在しない。** Trial 5 のピークは 0.589 で、その3%は 0.018 m/s。ベースライン（0.122）が最初のフレームから超えるため、**全NoGo試行でRTが出る**。相対基準は「スイングがあったかどうか」を判定する能力を持たない。
  - 3% × 26 = 0.78 m/s は、棄却した 0.75 とほぼ同じ水準。ピーク20 m/sの被験者なら 0.60 となり NoGo最大 0.589 をほぼ下回る（**閾値が被験者ごとに動くせいで安全マージンも動く**）。
  - 0.78 の交差は t≈0.50 s で、実際の動き出し t≈0.40 s より約100 ms遅く、**遅れの問題を解決していない**。
- **ただし逆探索の基準としてなら優秀**と説明。**同じ低い基準でも、前向きに探すか後ろ向きに探すかで安全性が全く違う**（アンカーから遡る場合、探索はスイング直前の谷で必ず止まり、それより前の身じろぎには到達しない）。これが案Bが「偽陽性の棄却」と「時刻の精度」を両立できる理由。
- 3つの逆探索基準（ベースライン+3SD / ピーク3% / ピーク1%）を比較するスクリプトを提示。

**決定事項・結果：**
- **free条件：RTは算出するが反応時間としては報告しない**（条件間のRT比較には含めず、②③の比較にのみ使う）と決定。MDに記録。

### 10. 逆探索基準の比較（S01）

**指示・質問：**
> （案A / B-1 / B-2 / B-3 の比較結果を貼付）

**AIの対応：**
- **アンカーが完璧に機能**したと報告：NoGo 4試行（5, 9, 13, 19）が全案で棄却され、ステップ1で見つかった偽陽性の問題が解決。
- **案B-1（ベースライン+3SD）を棄却**：simple Trial 7 で 52 ms、gonogo Trial 14 で 88 ms（生理的下限 約150 ms を大きく下回る）、simple Trial 8 で NaN。原因は基準 0.17 がベースライン 0.122 に近すぎること（谷が落ちきらない試行→NaN、準備動作を拾う試行→52 ms）。
- **ステップ2 の結論を一部撤回**：案B-1 の失敗は「キュー直後から低速の準備動作が存在する」ことを意味する。ステップ2で「中間の盛り上がりがない」と結論したのは、Trial 1 を縦軸 0〜3 m/s で見ての判断であり、**0.17〜1.0 m/s の帯域が見えていなかった**。加藤が当初から懸念していた「並進速度は体重移動でも上がる」リスクは**実在した**。
- **案B-2（ピーク3%）を棄却**：案Aとの差が15〜25 msしかなく、逆探索を入れる意味がない。
- **案B-3（ピーク1%）を提案**：simple -91.8 ms・gonogo -84.5 ms と全試行でほぼ一定だけ早く検出。外れ値もNaNもなし。**ピーク相対という加藤の発想がここで効いている**（被験者間でスイング規模が違っても同じ「動作の進行度」で揃う）と説明。
- free Trial 9 のみ挙動が違う（差348 ms）ため、波形確認を依頼。

**決定事項・結果：**
- MDに撤回ブロックと教訓（波形の観察は判断したい現象のスケールに合わせて縦軸を絞ること）を記録。

### 11. free Trial 9 の波形 ― 2段階構造の発見

**指示・質問：**
> （free Trial 9 の拡大波形の画像）結果が出ました

**AIの対応：**
- **2段階の構造**を読み取り：0.3 s から緩やかに立ち上がり（第1段＝準備動作）、0.45〜0.7 s で 0.28〜0.33 m/s の台を作り、0.68 s で谷、0.7 s から急峻に立ち上がる（第2段＝スイング本体）。
- **案B-3（460 ms）は第1段、案A（808 ms）は第2段**を拾っており、どちらも間違いではなく**測っているものが違う**と説明。
- **案B-3 が紙一重である理由**を特定：1%peak（0.26）が準備動作の台（0.28〜0.33）のすぐ下を通っており、0.68 s の谷（0.28）も 0.26 まで落ちきらないため、逆探索が谷で止まれず台をすり抜けた。**台の高さが基準をまたぐかどうかで結果が 460 か 680 か、220 ms も変わる。**
- simple/gonogo で案B-3 が安定していたのは、あの条件では準備動作が 0.26 より下に収まっていたためであり、**0.26 は原理的な根拠ではなく、たまたま挟まっていただけ**と指摘。
- 台の高さに依存しない**案B-4（アンカー直前の極小値）**を提案。

**決定事項・結果：**
- MDに記録。案B-3 と案B-4 の比較を依頼。

### 12. 案B-3 vs 案B-4 の比較 ― 案B-3 の採用

**指示・質問：**
> （案A / B-3 / B-4 の比較結果を貼付）

**AIの対応：**
- **案B-4 を棄却**：`B4-A` が -12 ms の試行が多数（simple Trial 4・10・7・12、free Trial 7・14・16）。極小値がアンカーの3フレーム手前にあり、**まったくさかのぼれていない**。予告どおり、急峻な立ち上がり途中のリップルを極小値として拾って止まる。失敗率は simple 4/20、free 5〜6/20 で**約20%**。gonogo だけ無傷だったのは偶然。
- **失敗率の比較**：案B-3 は 1/56（free Trial 9 のみ）、案B-4 は 9/56。案B-3 の勝ち。しかも案B-3 の唯一の失敗が free に出ているのは、free の RT を報告しないと決めたため実害がない。
- **精度の裏取り**：simple Trial 1 で 案A 516 / 案B-3 432 / 案B-4 372 に対し、目視で読んだ真の動き出しは t≈400 ms。案B-3・案B-4 とも±30 ms圏内で精度は同等なので、安定している案B-3 を選ぶべきと説明。
- 残る条件として**被験者2での検証**と**x5での全試行目視**を提示。

**決定事項・結果：**
- **案B-3（アンカー 1.0 m/s ＋ ピーク速度1%への逆探索）を採用。** MDに4案の比較・失敗率・精度の裏取り・確定した実装を記録。

### 13. スクリプトのリネーム

**指示・質問：**
> s2c_compare_onset_criteria.m これどこにありますか？
> ファイル名を変えておいてもらえますか？

**AIの対応：**
- **存在しないことを報告**。チャットに書いたコードにファイル名を付けて呼んでいただけで、ユーザーはコマンドウィンドウで実行していた。実ファイルは `re_analyze.m`・`re_check.m`・`determine_baseline.m` の3つで、`determine_baseline.m` が `s2c` に相当すると対応表で提示。
- ファイル名と中身がずれている点（`determine_baseline.m` が実際には検出基準の比較をしている）を指摘し、リネームを実施。
- 案B-4 の比較スクリプトはコマンドウィンドウ実行のため残っていないことを報告。

**決定事項・結果：**
- リネーム：`re_analyze.m` → `s2a_check_velocity_waveform.m`、`re_check.m` → `s2b_decide_threshold.m`、`determine_baseline.m` → `s2c_compare_onset_criteria.m`。中のヘッダーコメントも実名に修正（コードのロジックには不介入）。
- MDの「検討用スクリプトは `s4_SingleTrialAnalysisScratch/` に配置した」という**事実と違う記述を訂正**し、実際の置き場所・3ファイルの対応表・カレントディレクトリの注意に差し替え。

### 14. S02 での検証 ― 案B-3 の破綻

**指示・質問：**
> （S02 での案A / B-1 / B-2 / B-3 の比較結果を貼付）

**AIの対応：**
- **2箇所の独立した失敗**を報告：
  1. **アンカーがNoGoを棄却できていない。** S02 では 5試行中4試行でアンカーが立った（Trial 9: 1880 ms、13: 436 ms、17: 540 ms、20: 544 ms）。Trial 13・17 は Go試行の案A（364〜576 ms）と区別がつかない。
  2. **案B-3 が NaN（free 8/20、simple 5/20）と 0.0/4.0/24.0 ms を量産。** NaN と 0.0 は同じ原因の裏表で、どちらも **S02 はキューの時点でバットが止まっていない**ことを意味する。
- 決定的な証拠として free Trial 6 の案A = **112 ms**（生理的下限を下回る＝キューを見る前から動いていた）を提示。
- 診断：**S02 はバットを常時揺らして構えるタイプの被験者**で、「静止→動き出し」の前提が成立しない。閾値の調整では直らない。
- **NoGo判定は救える**として、ピーク速度による判定を提案（本物のスイング 25 m/s vs ワッグル 1〜3 m/s で10倍以上の差）。
- **動作開始の検出は救えない**として、RT検出の信号を Fz に移す可能性に言及。ただし判断前に S02 の波形を見るべきとして、`s2b`（S02のベースライン統計）と `s2a`（波形、`ylim([0 1.5])`に絞る）の実行を依頼。

**決定事項・結果：**
- 説明・確認依頼のみ。

### 15. S02 のベースライン統計 ― 破綻の確定と方針転換の提案

**指示・質問：**
> （S02 の確認1〜3 の実行結果を貼付）上記がs2bの実行結果です。
> Trial  1: ベースライン（キュー前0.5s） 平均 0.122, SD 0.015, 最大 0.173 m/s
> s2aは実行してもグラフが出てきませんでした。数値は上記のとおりです。

**AIの対応：**
- **貼られた s2a の数値が S01 の古い出力である**ことを指摘（S02 simple のベースライン平均は s2b では 0.240 であり、0.122 と両立しない）。s2a はエラーで止まっているか未実行の可能性が高いと報告。
- **s2b の結果だけで結論が出た**として、**成立する閾値が存在しない**ことを提示：
  - NoGoを棄却するには閾値は **1.533 m/s** より上でなければならない
  - ところが simple のキュー前ベースラインは **1.154 m/s** に達する（アンカー 1.0 を超える）
- S01 と S02 の比較表を提示（ベースライン平均 2.3倍、ベースライン最大の最大 3.1倍、NoGo最大 2.6倍）。
- 決定的な証拠を2点提示：
  1. 閾値 0.3 での平均RTが free **143.4 ms**・simple 182.0 ms（生理的下限 約150 ms を下回る）
  2. 閾値 0.5→2.0 でのRTの広がりが S01 simple 111 ms に対し **S02 simple 326 ms**（立ち上がりが急峻でなく、閾値の選択が恣意的になる）
- **救えるもの（NoGo判定 → ピーク速度で、16倍のマージン）と救えないもの（動作開始の検出）**を切り分け。
- **RT検出をFzに移す方針転換**を提案。利点3つ（ワッグルはFzに影響しない／既に③に「立ち上がり時刻」が入っており工数が減る／体重移動はスイングより先に起こる）と懸念2つ（「スイング開始」ではなく「動作開始」の定義になる／NoGoでの部分反応をFzが拾う可能性）を提示。
- 判断前に Fz のベースラインを測る `s2e_check_fz_baseline.m` を提示（バット速度と同じ土俵＝ベースラインSDとNoGo最大逸脱の比で評価）。
- MDに、破綻の証拠4点・S01/S02比較表・救えるもの/救えないものの切り分け・Fzへの方針転換案・教訓2点（「1人目で成立した基準は2人目で成立するとは限らない」「コマンドウィンドウの残存出力に注意」）を記録。「2. 解析項目」の決定事項にも、S02で前提が崩れた旨の見直しブロックを追加。

**決定事項・結果：**
- 更新：`予備実験解析し直し/00_解析し直し方針と作業記録.md`。
- **バット先端速度によるRT検出は S02 で破綻**と結論。方針転換は先行研究の調査後に判断することとした。

### 16. セッション終了とログ記載

**指示・質問：**
> 反応時間の算出をバットマーカーから出す方法をしている先行研究を探してみます。
> 一旦ここまででログを書いておいてください。

**AIの対応：**
- `date` コマンドで当日が 2026-07-16 であることを確認。本日は `2026-07-16_01.md` が既にあるため、連番で `2026-07-16_02.md` として本ログを作成。
- `log/00_logging_policy.md` の様式に従い、概要欄・備考欄・やり取り1〜16 を記載。

**決定事項・結果：**
- 新規作成：`log/2026-07-16_02.md`（本ログ）。
