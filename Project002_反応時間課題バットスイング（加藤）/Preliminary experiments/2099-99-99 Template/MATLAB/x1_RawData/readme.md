
## x1_RawData — 生データ

ワークフロー ステップ1に対応。

Qualisys で計測した生データをそのままコピーして保存するフォルダ。このフォルダ内のファイルは編集しない。

### 格納するもの
- Qualisys からエクスポートした `.mat` ファイル

### 注意
- 生データは上書き・削除しないこと
- ファイル名はエクスポート時のままにしておく

### 次のステップ
`x2_Data` でクリーニング処理を行う。
