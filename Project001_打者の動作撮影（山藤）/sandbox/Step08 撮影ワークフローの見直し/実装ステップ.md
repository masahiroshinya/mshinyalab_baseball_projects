# Step08 実装ステップ：撮影ワークフローの見直し

このドキュメントは、worklist.md に基づき、3つのソフトウェア（映像同期・キャリブレーション画像取得・分析対象映像切り出し）を Google Colab 上でプロトタイプ開発するための具体的な実装手順です。

## 作業の概要と工数

| ソフトウェア | 概要 | 想定工数 |
|:---|:---|:---|
| ソフト1：映像同期 | LED輝度変化のクロスコリレーションで映像間のフレームオフセットを計算し、同期クリップを生成 | 約2〜3時間 |
| ソフト2：キャリブレーション画像取得 | 同期済み映像からチェスボードのコーナー検出に成功するフレームを選択・保存 | 約1〜2時間 |
| ソフト3：分析対象映像切り出し | 同期済み映像から分析開始/終了フレームを選択し、フレーム番号をCSV保存 | 約30分〜1時間 |

**開発方針:**
- Google Colab 上でプロトタイプを開発する
- 処理ロジック（関数）と UI（ipywidgets）を分離して実装する
- ファイルパスは `#@param` を使ったフォーム入力にする（ハードコード禁止）
- カメラ台数は `nCamera` として汎用的に扱う（当面は2台で開発）

---

# 同期用LEDの推奨仕様

## なぜLEDで同期をとるのか

OSMO Action 3 は 240fps 撮影時にタイムコード同期機能が使えない（60fps以上では無効）。
そのため、全カメラの画角に映る**共通の視覚イベント（LED点灯）**を利用してソフトウェア的にフレーム同期をとる。

## LED の選定基準

| 項目 | 推奨仕様 | 理由 |
|:---|:---|:---|
| **色** | **白色** または **暖白色**（3000〜6500K） | カラーフィルタの影響を受けにくく、グレースケール変換後の輝度コントラストが最大になる |
| **明るさ** | **100〜300ルーメン程度**（懐中電灯クラス） | 屋外でも映像上で明確な輝度変化が得られる。暗すぎると背景光に埋もれる |
| **大きさ（発光面）** | **直径 1〜3 cm** 程度の点光源 | 映像上で 10〜30 ピクセル程度のROIに収まるサイズ。大きすぎると輝度変化がなだらかになる |
| **スイッチ方式** | **押しボタン式**（押している間だけ点灯） | ランダム間隔での複数回点灯が容易。トグル式は操作ミスが起きやすい |
| **応答速度** | **即時点灯**（LED固有の特性で問題なし） | 白熱球やフィラメント式は立ち上がり遅延があるため不可 |
| **電源** | 電池式（単三/単四 または USB充電式） | 撮影現場での取り回しの良さ |

## 具体的な製品例

| 分類 | 製品例 | 概算価格 | 備考 |
|:---|:---|:---|:---|
| **最も手軽** | スマホのフラッシュライト | 0円 | 一番簡単。ただし明るさ調整ができない場合がある |
| **推奨** | 小型LEDペンライト（100〜200ルーメン） | 500〜2,000円 | ボタン押下で即時点灯、持ちやすい。100均でも可 |
| **高精度向け** | USB接続LEDボタン（Arduino + LED） | 1,000〜3,000円 | 将来的に自動化する場合の拡張性がある |

## 撮影時の注意事項

1. **カメラの露出は手動（固定）に設定する** — 自動露出だとLED点灯時にカメラが露出を補正し、輝度変化が平滑化されてしまう
2. **LEDは全カメラの画角内に映る位置に置く** — 各カメラから見えるならば、LEDの向きは厳密でなくてよい
3. **複数回・ランダム間隔で点灯する** — 等間隔だとクロスコリレーションに複数ピークが生じ、誤同期の原因になる
4. **1回あたりの点灯時間は 0.3〜1秒**程度 — 短すぎると240fpsでも数フレームしか映らず不安定。長すぎるとエッジがぼやける
5. **最低3〜5回は点灯させる** — 回数が多いほどクロスコリレーションのSN比が向上する

---

# ソフト1：映像間の同期をとり、クリップする

## 概要

複数カメラの映像に映り込んだLEDの輝度変化を利用し、カメラ間のフレームオフセットを計算する。オフセットに基づき全カメラ共通の時間領域のみをクリップし、同期済み映像ファイルとして保存する。

## 作業工程（全9ステップ）

| ステップ | 内容 | 想定時間 | 備考 |
|:---|:---|:---|:---|
| 1-1 | ライブラリのインポート | 1分 | コピペのみ |
| 1-2 | 動画ファイルの読み込みと基本情報取得 | 5分 | パスを入力して実行 |
| 1-3 | FPS整合性チェック | 2分 | 全カメラのFPSが一致するか確認 |
| 1-4 | 1フレーム目の表示 | 5分 | LEDが映っているか目視確認 |
| 1-5 | LED ROIの座標入力 | 5分 | クリックまたは手入力で座標指定 |
| 1-6 | ROI内の輝度時系列の抽出 | 10〜30分 | 全フレーム走査（動画長に依存） |
| 1-7 | 輝度時系列のグラフ表示と目視確認 | 5分 | LEDパルスが見えるか確認 |
| 1-8 | 微分信号の生成とクロスコリレーションでオフセット計算 | 5分 | 自動処理＋結果確認 |
| 1-9 | 映像のクリップと同期済みファイルの保存 | 5〜15分 | 動画長に依存 |

**合計: 約45分〜1.5時間**（動画の長さとアップロード速度に依存）

---

## 手順1-1：動画ファイルの読み込みと基本情報の取得

動画ファイルのパスを入力し、フレーム数・FPS・解像度などの基本情報を取得する。

```python
import cv2
import numpy as np

def get_video_info(path):
    """動画ファイルの基本情報を取得する"""
    cap = cv2.VideoCapture(path)
    if not cap.isOpened():
        print(f"Error: Could not open video file {path}")
        return None

    info = {
        "path": path,
        "fps": cap.get(cv2.CAP_PROP_FPS),
        "width": int(cap.get(cv2.CAP_PROP_FRAME_WIDTH)),
        "height": int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT)),
        "n_frames": int(cap.get(cv2.CAP_PROP_FRAME_COUNT)),
    }
    info["duration_sec"] = info["n_frames"] / info["fps"] if info["fps"] > 0 else 0
    cap.release()
    return info

#@title 動画ファイルの選択 { display-mode: "form" }
camera1_path = "/content/cam1.MP4" #@param {type:"string"}
camera2_path = "/content/cam2.MP4" #@param {type:"string"}

video_paths = [camera1_path, camera2_path]
video_infos = []
for i, path in enumerate(video_paths):
    info = get_video_info(path)
    if info is None:
        raise FileNotFoundError(f"Camera {i+1} の動画ファイルが見つかりません: {path}")
    video_infos.append(info)
    print(f"Camera {i+1}: FPS={info['fps']}, {info['width']}x{info['height']}, "
          f"{info['n_frames']}frames, {info['duration_sec']:.1f}sec")

nCamera = len(video_paths)
print(f"\nカメラ台数: {nCamera}")
```

**確認ポイント：**
- 全カメラのFPSが同一であること
- 各動画ファイルが正常に読み込まれていること

---

## 手順1-2：1フレーム目の表示とLED ROIのクリック指定

各カメラの1フレーム目を横並びで表示し、ユーザーがLEDの位置をクリックしてROIを指定する。

```python
import matplotlib.pyplot as plt
from matplotlib.widgets import Cursor
from IPython.display import display, clear_output
import ipywidgets as widgets

def read_first_frame(path):
    """動画の1フレーム目を読み込む"""
    cap = cv2.VideoCapture(path)
    ret, frame = cap.read()
    cap.release()
    if not ret:
        raise RuntimeError(f"フレーム読み込み失敗: {path}")
    return cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)

def select_roi_click(frames, roi_size=30):
    """
    各カメラのフレームを表示し、クリックでLEDのROI中心を指定する。
    roi_size: ROIの半径（ピクセル）

    Returns: list of dict {"x": int, "y": int, "roi_size": int}
    """
    roi_centers = []

    for i, frame in enumerate(frames):
        print(f"--- Camera {i+1}: LEDの位置をクリックしてください ---")
        fig, ax = plt.subplots(1, 1, figsize=(12, 8))
        ax.imshow(frame)
        ax.set_title(f"Camera {i+1} - LEDの位置をクリック")

        clicked = []

        def onclick(event):
            if event.inaxes == ax:
                cx, cy = int(event.xdata), int(event.ydata)
                clicked.append({"x": cx, "y": cy, "roi_size": roi_size})
                # ROI矩形を描画
                rect = plt.Rectangle(
                    (cx - roi_size, cy - roi_size),
                    roi_size * 2, roi_size * 2,
                    linewidth=2, edgecolor='red', facecolor='none'
                )
                ax.add_patch(rect)
                ax.plot(cx, cy, 'r+', markersize=15, markeredgewidth=2)
                fig.canvas.draw()
                print(f"  ROI中心: ({cx}, {cy})")

        cid = fig.canvas.mpl_connect('button_press_event', onclick)
        plt.show()

        # Colab環境ではインタラクティブバックエンドが必要
        # %matplotlib widget を事前に実行すること
        # クリック後、次のセルに進む

        if len(clicked) > 0:
            roi_centers.append(clicked[-1])  # 最後のクリックを採用
        else:
            print(f"  ⚠ Camera {i+1} でクリックが検出されませんでした")
            roi_centers.append({"x": 0, "y": 0, "roi_size": roi_size})

    return roi_centers

# 1フレーム目の読み込み
first_frames = [read_first_frame(p) for p in video_paths]

# Colab でインタラクティブプロットを有効にする
# %matplotlib widget

# ROI選択（注意: Colab上では %matplotlib widget が必要）
roi_centers = select_roi_click(first_frames, roi_size=30)
print("\n--- ROI設定結果 ---")
for i, roi in enumerate(roi_centers):
    print(f"Camera {i+1}: 中心=({roi['x']}, {roi['y']}), ROIサイズ={roi['roi_size']}")
```

**確認ポイント：**
- `%matplotlib widget` を事前に実行してインタラクティブモードにすること
- クリック位置にLEDが含まれていること（赤い矩形で確認）

---

## 手順1-3：ROI内の輝度時系列の抽出

指定したROI内のピクセル輝度の平均値を全フレームにわたって取得し、時系列データを生成する。

```python
def extract_roi_brightness(video_path, roi_center, roi_size=30):
    """
    動画のROI内の平均輝度を全フレームにわたって取得する。

    Args:
        video_path: 動画ファイルのパス
        roi_center: dict with "x", "y" keys
        roi_size: ROIの半径

    Returns:
        brightness: numpy array of mean brightness per frame
    """
    cap = cv2.VideoCapture(video_path)
    n_frames = int(cap.get(cv2.CAP_PROP_FRAME_COUNT))
    width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
    height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))

    cx, cy = roi_center["x"], roi_center["y"]
    x1 = max(0, cx - roi_size)
    y1 = max(0, cy - roi_size)
    x2 = min(width, cx + roi_size)
    y2 = min(height, cy + roi_size)

    brightness = np.zeros(n_frames)

    for f in range(n_frames):
        ret, frame = cap.read()
        if not ret:
            break
        gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
        roi = gray[y1:y2, x1:x2]
        brightness[f] = np.mean(roi)

    cap.release()
    print(f"  輝度抽出完了: {len(brightness)} frames, ROI=({x1},{y1})-({x2},{y2})")
    return brightness

# 全カメラの輝度時系列を取得
brightness_list = []
for i in range(nCamera):
    print(f"Camera {i+1} の輝度を抽出中...")
    b = extract_roi_brightness(video_paths[i], roi_centers[i], roi_centers[i]["roi_size"])
    brightness_list.append(b)

# 時系列をプロット
fig, axes = plt.subplots(nCamera, 1, figsize=(14, 3 * nCamera), sharex=True)
if nCamera == 1:
    axes = [axes]
for i, b in enumerate(brightness_list):
    axes[i].plot(b, linewidth=0.5)
    axes[i].set_ylabel(f"Cam {i+1}\nBrightness")
    axes[i].set_title(f"Camera {i+1} - ROI輝度時系列")
axes[-1].set_xlabel("Frame")
plt.tight_layout()
plt.show()
```

**確認ポイント：**
- LEDの点灯・消灯がパルス状に見えること
- 各カメラの波形が類似したパターンを示すこと（オフセットあり）

---

## 手順1-4：微分信号の生成とクロスコリレーションによるオフセット計算

輝度時系列を微分してエッジ信号に変換し、カメラ1を基準にクロスコリレーションでフレームオフセットを計算する。

```python
from scipy import signal

def compute_offset_by_cross_correlation(brightness_ref, brightness_target):
    """
    微分信号のクロスコリレーションにより、フレームオフセットを計算する。

    Args:
        brightness_ref: 基準カメラの輝度時系列（numpy array）
        brightness_target: 対象カメラの輝度時系列（numpy array）

    Returns:
        offset: フレーム単位のオフセット（正: targetが遅れている、負: targetが進んでいる）
        correlation: クロスコリレーション結果
    """
    # 微分信号（エッジ検出）
    diff_ref = np.diff(brightness_ref)
    diff_target = np.diff(brightness_target)

    # 正規化
    diff_ref = (diff_ref - np.mean(diff_ref)) / (np.std(diff_ref) + 1e-8)
    diff_target = (diff_target - np.mean(diff_target)) / (np.std(diff_target) + 1e-8)

    # クロスコリレーション
    correlation = signal.correlate(diff_ref, diff_target, mode='full')
    lags = signal.correlation_lags(len(diff_ref), len(diff_target), mode='full')

    # 最大相関のラグ = オフセット
    offset = lags[np.argmax(correlation)]

    return offset, correlation, lags

# カメラ1を基準にオフセットを計算
offsets = [0]  # Camera 1 のオフセットは 0
print("=== オフセット計算結果 ===")
print(f"Camera 1: 基準 (offset = 0)")

for i in range(1, nCamera):
    offset, corr, lags = compute_offset_by_cross_correlation(
        brightness_list[0], brightness_list[i]
    )
    offsets.append(offset)
    fps = video_infos[0]["fps"]
    print(f"Camera {i+1}: offset = {offset} frames ({offset/fps*1000:.1f} ms)")

# 微分信号とクロスコリレーション結果の可視化
fig, axes = plt.subplots(2, 1, figsize=(14, 6))

# 微分信号
for i in range(nCamera):
    diff_signal = np.diff(brightness_list[i])
    axes[0].plot(diff_signal, label=f"Cam {i+1}", alpha=0.7, linewidth=0.5)
axes[0].set_title("微分信号（輝度変化のエッジ）")
axes[0].set_xlabel("Frame")
axes[0].legend()

# クロスコリレーション（Camera 1 vs Camera 2）
if nCamera >= 2:
    _, corr, lags = compute_offset_by_cross_correlation(
        brightness_list[0], brightness_list[1]
    )
    axes[1].plot(lags, corr)
    axes[1].axvline(x=offsets[1], color='r', linestyle='--', label=f"offset={offsets[1]}")
    axes[1].set_title("クロスコリレーション (Cam1 vs Cam2)")
    axes[1].set_xlabel("Lag (frames)")
    axes[1].legend()

plt.tight_layout()
plt.show()
```

**確認ポイント：**
- クロスコリレーションに明確なピークがあること
- オフセットが合理的な値であること（期待精度: ±1フレーム ≈ 4ms @ 240fps）

---

## 手順1-5：映像のクリップと同期済みファイルの保存

計算したオフセットに基づき、全カメラで共通する時間領域のみをクリップし、同期済み映像ファイルとして保存する。

```python
def clip_and_save_synced_videos(video_paths, offsets, video_infos, output_prefix="cam"):
    """
    オフセットに基づき映像をクリップし、同期済みファイルとして保存する。

    Args:
        video_paths: 動画ファイルパスのリスト
        offsets: 各カメラのフレームオフセット（Camera1基準）
        video_infos: 各カメラのビデオ情報
        output_prefix: 出力ファイルの接頭辞

    Returns:
        output_paths: 出力ファイルパスのリスト
    """
    nCamera = len(video_paths)

    # 各カメラの有効開始フレーム・終了フレームを計算
    # offset > 0: refがtargetより先に始まっている → targetを先頭に合わせる
    start_frames = []
    for i in range(nCamera):
        # Camera i の映像内で、同期後に切り出しを開始するフレーム番号
        start = max(0, offsets[i])
        start_frames.append(start)

    # 全カメラで共通する最大フレーム数
    available_frames = []
    for i in range(nCamera):
        avail = video_infos[i]["n_frames"] - start_frames[i]
        available_frames.append(avail)
    common_frames = min(available_frames)

    print(f"=== クリップ情報 ===")
    for i in range(nCamera):
        print(f"Camera {i+1}: 開始フレーム={start_frames[i]}, "
              f"利用可能={available_frames[i]}frames")
    print(f"共通フレーム数: {common_frames}")

    # 映像のクリップと保存
    fps = video_infos[0]["fps"]
    width = video_infos[0]["width"]
    height = video_infos[0]["height"]
    fourcc = cv2.VideoWriter_fourcc(*'mp4v')

    output_paths = []
    for i in range(nCamera):
        output_path = f"{output_prefix}{i+1}_synced.mp4"
        output_paths.append(output_path)

        cap = cv2.VideoCapture(video_paths[i])
        out = cv2.VideoWriter(output_path, fourcc, fps, (width, height))

        # 開始フレームまでスキップ
        cap.set(cv2.CAP_PROP_POS_FRAMES, start_frames[i])

        for f in range(common_frames):
            ret, frame = cap.read()
            if not ret:
                print(f"  ⚠ Camera {i+1}: フレーム {start_frames[i]+f} で読み込み失敗")
                break
            out.write(frame)

        cap.release()
        out.release()
        print(f"Camera {i+1}: {output_path} を保存しました ({common_frames} frames)")

    return output_paths

# 同期クリップの生成
output_paths = clip_and_save_synced_videos(video_paths, offsets, video_infos)

print("\n=== 出力ファイル ===")
for p in output_paths:
    print(f"  {p}")
```

**確認ポイント：**
- 出力された同期済み映像を再生し、LEDの点灯タイミングが揃っていること
- 全カメラの映像が同じ長さであること

---

# ソフト2：キャリブレーション画像を取得する

## 概要

同期済み映像セットを入力として、チェスボードのコーナー検出に成功するフレームをユーザーが選択し、キャリブレーション用の画像ファイルとして保存する。

**処理の流れ:**
1. 同期済み映像の読み込み
2. 全カメラ映像の横並び表示 + スライダー/コマ送りでフレーム選択
3. 選択フレームでコーナー検出を実行
4. 検出成功したフレームの画像をフル解像度で保存
5. 選択フレーム番号をCSVとして保存

---

## 手順2-1：同期済み映像の読み込み

```python
#@title 同期済み映像の選択 { display-mode: "form" }
synced_cam1_path = "/content/cam1_synced.mp4" #@param {type:"string"}
synced_cam2_path = "/content/cam2_synced.mp4" #@param {type:"string"}

synced_paths = [synced_cam1_path, synced_cam2_path]
synced_infos = []
for i, path in enumerate(synced_paths):
    info = get_video_info(path)
    if info is None:
        raise FileNotFoundError(f"同期済みファイルが見つかりません: {path}")
    synced_infos.append(info)
    print(f"Synced Camera {i+1}: {info['n_frames']}frames, "
          f"{info['width']}x{info['height']}, {info['fps']}fps")
```

---

## 手順2-2：フレームブラウザ（横並び表示 + スライダー + コマ送り）

全カメラの映像を横並びで表示し、スライダーとボタンでフレームを操作して、キャリブレーションに使うフレームを選択する。

```python
import ipywidgets as widgets
from IPython.display import display, clear_output

#@title キャリブレーション設定 { display-mode: "form" }
nRequiredFrames = 10 #@param {type:"integer"}
nMaxFrames = 30 #@param {type:"integer"}
chess_rows = 6 #@param {type:"integer"}
chess_cols = 9 #@param {type:"integer"}

class FrameBrowser:
    """全カメラの映像を横並び表示し、フレーム選択を行うUI"""

    def __init__(self, video_paths, video_infos, scale=0.3):
        self.caps = [cv2.VideoCapture(p) for p in video_paths]
        self.infos = video_infos
        self.nCamera = len(video_paths)
        self.n_frames = min(info["n_frames"] for info in video_infos)
        self.scale = scale
        self.selected_frames = []

        # UI部品
        self.slider = widgets.IntSlider(
            value=0, min=0, max=self.n_frames - 1,
            step=1, description='Frame:',
            layout=widgets.Layout(width='80%')
        )
        self.btn_prev = widgets.Button(description='◀ -1')
        self.btn_next = widgets.Button(description='+1 ▶')
        self.btn_prev10 = widgets.Button(description='◀◀ -10')
        self.btn_next10 = widgets.Button(description='+10 ▶▶')
        self.btn_select = widgets.Button(
            description='✅ このフレームを選択',
            button_style='success'
        )
        self.btn_undo = widgets.Button(
            description='↩ 最後の選択を取消',
            button_style='warning'
        )
        self.output = widgets.Output()
        self.status = widgets.HTML()

        # イベント接続
        self.slider.observe(self._on_slider_change, names='value')
        self.btn_prev.on_click(lambda _: self._step(-1))
        self.btn_next.on_click(lambda _: self._step(1))
        self.btn_prev10.on_click(lambda _: self._step(-10))
        self.btn_next10.on_click(lambda _: self._step(10))
        self.btn_select.on_click(self._on_select)
        self.btn_undo.on_click(self._on_undo)

    def show(self):
        """UIを表示する"""
        nav_buttons = widgets.HBox([
            self.btn_prev10, self.btn_prev,
            self.btn_next, self.btn_next10
        ])
        action_buttons = widgets.HBox([self.btn_select, self.btn_undo])
        display(self.slider, nav_buttons, action_buttons, self.status, self.output)
        self._update_display()

    def _step(self, delta):
        new_val = max(0, min(self.n_frames - 1, self.slider.value + delta))
        self.slider.value = new_val

    def _on_slider_change(self, change):
        self._update_display()

    def _on_select(self, _):
        frame_no = self.slider.value
        if frame_no in self.selected_frames:
            self.status.value = f"<b style='color:orange'>⚠ フレーム {frame_no} は既に選択済みです</b>"
            return
        if len(self.selected_frames) >= nMaxFrames:
            self.status.value = f"<b style='color:red'>❌ 最大選択数 ({nMaxFrames}) に達しています</b>"
            return
        self.selected_frames.append(frame_no)
        self._update_status()

    def _on_undo(self, _):
        if self.selected_frames:
            removed = self.selected_frames.pop()
            self.status.value = f"<b>↩ フレーム {removed} の選択を取り消しました</b>"
        self._update_status()

    def _update_status(self):
        n = len(self.selected_frames)
        color = 'green' if n >= nRequiredFrames else 'blue'
        self.status.value = (
            f"<b style='color:{color}'>選択済み: {n} / {nRequiredFrames}〜{nMaxFrames} フレーム</b>"
            f"<br>選択フレーム: {self.selected_frames}"
        )

    def _update_display(self):
        frame_no = self.slider.value
        frames = []
        for i, cap in enumerate(self.caps):
            cap.set(cv2.CAP_PROP_POS_FRAMES, frame_no)
            ret, frame = cap.read()
            if ret:
                h, w = frame.shape[:2]
                small = cv2.resize(frame, (int(w * self.scale), int(h * self.scale)))
                frames.append(cv2.cvtColor(small, cv2.COLOR_BGR2RGB))

        if frames:
            combined = np.hstack(frames)
            with self.output:
                clear_output(wait=True)
                fig, ax = plt.subplots(1, 1, figsize=(16, 5))
                ax.imshow(combined)
                ax.set_title(f"Frame {frame_no} / {self.n_frames - 1}")
                ax.axis('off')
                plt.tight_layout()
                plt.show()

    def close(self):
        for cap in self.caps:
            cap.release()

# フレームブラウザを起動
browser = FrameBrowser(synced_paths, synced_infos, scale=0.3)
browser.show()
```

**操作方法：**
- スライダーでフレームを移動
- ◀/▶ ボタンで1フレームずつコマ送り
- ◀◀/▶▶ ボタンで10フレームずつジャンプ
- 「✅ このフレームを選択」でフレームを保存候補に追加
- 最低 `nRequiredFrames`（10）枚以上選択すること

---

## 手順2-3：コーナー検出と検証

選択したフレームに対して全カメラでチェスボードのコーナー検出を実行し、失敗したフレームを警告する。

```python
def detect_corners_for_frames(video_paths, frame_numbers, chess_size):
    """
    指定フレームに対してコーナー検出を実行する。

    Args:
        video_paths: 動画パスのリスト
        frame_numbers: フレーム番号のリスト
        chess_size: (cols, rows) チェスボードの内部コーナー数

    Returns:
        results: dict {frame_no: {"success": [bool, ...], "corners": [corners, ...]}}
    """
    nCamera = len(video_paths)
    criteria = (cv2.TERM_CRITERIA_EPS + cv2.TERM_CRITERIA_MAX_ITER, 30, 0.001)
    results = {}

    for frame_no in frame_numbers:
        cam_results = {"success": [], "corners": []}

        for i, path in enumerate(video_paths):
            cap = cv2.VideoCapture(path)
            cap.set(cv2.CAP_PROP_POS_FRAMES, frame_no)
            ret, frame = cap.read()
            cap.release()

            if not ret:
                cam_results["success"].append(False)
                cam_results["corners"].append(None)
                continue

            gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
            found, corners = cv2.findChessboardCorners(gray, chess_size, None)

            if found:
                corners_refined = cv2.cornerSubPix(
                    gray, corners, (11, 11), (-1, -1), criteria
                )
                cam_results["success"].append(True)
                cam_results["corners"].append(corners_refined)
            else:
                cam_results["success"].append(False)
                cam_results["corners"].append(None)

        results[frame_no] = cam_results

        # 結果表示
        status_icons = ["✅" if s else "❌" for s in cam_results["success"]]
        all_ok = all(cam_results["success"])
        prefix = "  " if all_ok else "⚠ "
        cam_status = " | ".join([f"Cam{j+1}:{icon}" for j, icon in enumerate(status_icons)])
        print(f"{prefix}Frame {frame_no:>6d}: {cam_status}")

    return results

# コーナー検出を実行
chess_size = (chess_cols, chess_rows)
selected_frames = browser.selected_frames

print(f"=== コーナー検出 ({len(selected_frames)} フレームに対して実行) ===\n")
corner_results = detect_corners_for_frames(synced_paths, selected_frames, chess_size)

# 全カメラで成功したフレームのみ抽出
valid_frames = [
    fn for fn, r in corner_results.items()
    if all(r["success"])
]
failed_frames = [
    fn for fn, r in corner_results.items()
    if not all(r["success"])
]

print(f"\n--- 結果 ---")
print(f"全カメラで検出成功: {len(valid_frames)} フレーム")
print(f"一部失敗:         {len(failed_frames)} フレーム → {failed_frames}")

if len(valid_frames) < nRequiredFrames:
    print(f"\n⚠ 有効フレーム数が {nRequiredFrames} 枚に達していません。"
          f"フレームブラウザに戻り、追加のフレームを選択してください。")
```

**確認ポイント：**
- 全カメラで検出成功したフレーム数が `nRequiredFrames` 以上あること
- 失敗フレームがある場合は、フレームブラウザに戻って別のフレームを選び直す

---

## 手順2-4：キャリブレーション画像の保存

コーナー検出に成功したフレームの画像をフル解像度で保存する。

```python
import csv

def save_calibration_images(video_paths, valid_frames, output_dir=""):
    """
    有効フレームの画像をフル解像度で保存する。

    Args:
        video_paths: 動画パスのリスト
        valid_frames: 有効なフレーム番号のリスト
        output_dir: 出力ディレクトリ

    Returns:
        saved_files: 保存したファイルパスのリスト
    """
    nCamera = len(video_paths)
    saved_files = []

    for i, path in enumerate(video_paths):
        cap = cv2.VideoCapture(path)
        for j, frame_no in enumerate(valid_frames):
            cap.set(cv2.CAP_PROP_POS_FRAMES, frame_no)
            ret, frame = cap.read()
            if ret:
                filename = f"{output_dir}calib_cam{i+1}_{j+1:03d}.jpg"
                cv2.imwrite(filename, frame)
                saved_files.append(filename)
        cap.release()

    return saved_files

def save_frame_numbers_csv(valid_frames, filename="calib_frame_numbers.csv"):
    """フレーム番号をCSVとして保存する"""
    with open(filename, 'w', newline='') as f:
        writer = csv.writer(f)
        writer.writerow(["index", "frame_number"])
        for i, fn in enumerate(valid_frames):
            writer.writerow([i + 1, fn])
    print(f"フレーム番号を保存: {filename}")

# 画像の保存
saved = save_calibration_images(synced_paths, valid_frames)
print(f"\n保存した画像ファイル数: {len(saved)}")
for f in saved:
    print(f"  {f}")

# フレーム番号CSVの保存
save_frame_numbers_csv(valid_frames)

# フレームブラウザを閉じる
browser.close()
```

**確認ポイント：**
- `calib_cam1_001.jpg`, `calib_cam2_001.jpg` ... の形式で保存されていること
- `calib_frame_numbers.csv` が生成されていること
- Colab の場合は手動でダウンロードする

---

# ソフト3：分析対象映像を切り取る

## 概要

同期済み映像セットを入力として、バッティング動作の分析開始フレームと終了フレームをユーザーが指定し、フレーム番号のみをCSVとして保存する（映像ファイルは保存しない）。

---

## 手順3-1：分析区間の選択UI

```python
#@title 分析対象の同期済み映像 { display-mode: "form" }
analysis_cam1_path = "/content/cam1_synced.mp4" #@param {type:"string"}
analysis_cam2_path = "/content/cam2_synced.mp4" #@param {type:"string"}

analysis_paths = [analysis_cam1_path, analysis_cam2_path]
analysis_infos = [get_video_info(p) for p in analysis_paths]

class AnalysisFrameSelector:
    """分析開始/終了フレームを選択するUI"""

    def __init__(self, video_paths, video_infos, scale=0.3):
        self.caps = [cv2.VideoCapture(p) for p in video_paths]
        self.infos = video_infos
        self.nCamera = len(video_paths)
        self.n_frames = min(info["n_frames"] for info in video_infos)
        self.scale = scale
        self.start_frame = None
        self.end_frame = None

        # UI部品
        self.slider = widgets.IntSlider(
            value=0, min=0, max=self.n_frames - 1,
            step=1, description='Frame:',
            layout=widgets.Layout(width='80%')
        )
        self.btn_prev = widgets.Button(description='◀ -1')
        self.btn_next = widgets.Button(description='+1 ▶')
        self.btn_prev10 = widgets.Button(description='◀◀ -10')
        self.btn_next10 = widgets.Button(description='+10 ▶▶')
        self.btn_set_start = widgets.Button(
            description='🟢 開始フレーム設定',
            button_style='success'
        )
        self.btn_set_end = widgets.Button(
            description='🔴 終了フレーム設定',
            button_style='danger'
        )
        self.output = widgets.Output()
        self.status = widgets.HTML()

        # イベント接続
        self.slider.observe(self._on_slider_change, names='value')
        self.btn_prev.on_click(lambda _: self._step(-1))
        self.btn_next.on_click(lambda _: self._step(1))
        self.btn_prev10.on_click(lambda _: self._step(-10))
        self.btn_next10.on_click(lambda _: self._step(10))
        self.btn_set_start.on_click(self._on_set_start)
        self.btn_set_end.on_click(self._on_set_end)

    def show(self):
        nav_buttons = widgets.HBox([
            self.btn_prev10, self.btn_prev,
            self.btn_next, self.btn_next10
        ])
        action_buttons = widgets.HBox([self.btn_set_start, self.btn_set_end])
        display(self.slider, nav_buttons, action_buttons, self.status, self.output)
        self._update_display()

    def _step(self, delta):
        new_val = max(0, min(self.n_frames - 1, self.slider.value + delta))
        self.slider.value = new_val

    def _on_slider_change(self, change):
        self._update_display()

    def _on_set_start(self, _):
        self.start_frame = self.slider.value
        self._update_status()

    def _on_set_end(self, _):
        self.end_frame = self.slider.value
        self._update_status()

    def _update_status(self):
        start_str = str(self.start_frame) if self.start_frame is not None else "未設定"
        end_str = str(self.end_frame) if self.end_frame is not None else "未設定"

        duration = ""
        if self.start_frame is not None and self.end_frame is not None:
            n = self.end_frame - self.start_frame + 1
            fps = self.infos[0]["fps"]
            sec = n / fps if fps > 0 else 0
            color = 'green' if n > 0 else 'red'
            duration = f"<br><b style='color:{color}'>区間: {n} frames ({sec:.2f} sec)</b>"

        self.status.value = (
            f"<b>開始フレーム: {start_str} | 終了フレーム: {end_str}</b>"
            f"{duration}"
        )

    def _update_display(self):
        frame_no = self.slider.value
        frames = []
        for cap in self.caps:
            cap.set(cv2.CAP_PROP_POS_FRAMES, frame_no)
            ret, frame = cap.read()
            if ret:
                h, w = frame.shape[:2]
                small = cv2.resize(frame, (int(w * self.scale), int(h * self.scale)))
                frames.append(cv2.cvtColor(small, cv2.COLOR_BGR2RGB))

        if frames:
            combined = np.hstack(frames)
            with self.output:
                clear_output(wait=True)
                fig, ax = plt.subplots(1, 1, figsize=(16, 5))
                ax.imshow(combined)
                ax.set_title(f"Frame {frame_no} / {self.n_frames - 1}")
                ax.axis('off')
                plt.tight_layout()
                plt.show()

    def close(self):
        for cap in self.caps:
            cap.release()

# 分析区間セレクタを起動
selector = AnalysisFrameSelector(analysis_paths, analysis_infos, scale=0.3)
selector.show()
```

**操作方法：**
- スライダー/ボタンでフレームを移動
- 「🟢 開始フレーム設定」で分析開始位置を設定
- 「🔴 終了フレーム設定」で分析終了位置を設定

---

## 手順3-2：分析フレーム番号のCSV保存

```python
def save_task_frame_numbers(start_frame, end_frame, filename="task_frame_numbers.csv"):
    """分析開始/終了フレーム番号をCSVとして保存する"""
    if start_frame is None or end_frame is None:
        print("❌ 開始フレームまたは終了フレームが設定されていません")
        return
    if end_frame <= start_frame:
        print("❌ 終了フレームは開始フレームより後にしてください")
        return

    with open(filename, 'w', newline='') as f:
        writer = csv.writer(f)
        writer.writerow(["type", "frame_number"])
        writer.writerow(["start", start_frame])
        writer.writerow(["end", end_frame])

    n_frames = end_frame - start_frame + 1
    print(f"✅ 保存しました: {filename}")
    print(f"   開始: {start_frame}, 終了: {end_frame}, 区間: {n_frames} frames")

# CSVとして保存
save_task_frame_numbers(selector.start_frame, selector.end_frame)

# セレクタを閉じる
selector.close()
```

**確認ポイント：**
- `task_frame_numbers.csv` に開始/終了フレーム番号が記録されていること
- Colab の場合は手動でダウンロードする

---

# 全体のワークフローまとめ

```
撮影（マニュアル）
  ↓
ソフト1: 映像同期
  入力: 各カメラの生映像ファイル
  出力: cam1_synced.mp4, cam2_synced.mp4
  ↓
ソフト2: キャリブレーション画像取得
  入力: 同期済み映像（チェスボード撮影分）
  出力: calib_cam1_001.jpg, ..., calib_frame_numbers.csv
  ↓
ソフト3: 分析対象映像切り出し
  入力: 同期済み映像（バッティング撮影分）
  出力: task_frame_numbers.csv
  ↓
Step06〜07: キャリブレーション＆3D再構成（既存のノートブック）
```
