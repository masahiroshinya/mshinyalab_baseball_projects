# Step04：姿勢推定とバット検出の統合による打者の自動特定

作成日：2026-03-25
担当：山藤プロジェクト（進矢研究室）

---

## 目標

Step02 の `keypoints.csv`（人物の手首座標）と Step03 の `bat_boxes.csv`（バットの位置）を組み合わせ、
**「バットに最も近い手首を持つ人物」を打者として自動特定する**。

---

## 前提条件

- **実行環境：Google Colab**（T4 GPU は不要、CPU で実行可）
- 入力ファイル：
  - `keypoints.csv`（Step02 で保存済み）
  - `bat_boxes.csv`（Step03 で保存済み）
  - `img1.jpg`（Step02 と同じ入力画像）

---

## アルゴリズムの概要

```
1. keypoints.csv から「手首」のキーポイントを抽出
2. bat_boxes.csv のバットのバウンディングボックスを読み込む
3. 各人物の手首とバットとの最小距離を計算
4. 距離が最小の人物を「打者」と判定
```

### ポイント距離→矩形距離の計算方法

手首座標 (px, py) とバット矩形 (x1, y1, x2, y2) の距離は以下で計算する：

```
dx = max(x1 - px, 0, px - x2)
dy = max(y1 - py, 0, py - y2)
distance = sqrt(dx² + dy²)
```

手首がバット矩形の内側にある場合、距離 = 0 になる。

---

## ステップ一覧

| ステップ | 内容                              | 難易度 |
| -------- | --------------------------------- | ------ |
| 1        | ファイルのアップロード            | ★☆☆ |
| 2        | データの読み込み                  | ★☆☆ |
| 3        | 手首キーポイントの抽出            | ★☆☆ |
| 4        | バットとの距離計算                | ★★☆ |
| 5        | 打者の特定                        | ★☆☆ |
| 6        | 結果の可視化                      | ★★☆ |
| 7        | 結果のCSV保存（任意）             | ★☆☆ |

---

## ステップ 1：ファイルのアップロード

Colabのファイルパネルから以下の3ファイルをアップロードする。

| ファイル名        | 内容                     |
| ----------------- | ------------------------ |
| `keypoints.csv`   | Step02 で保存した手首データ |
| `bat_boxes.csv`   | Step03 で保存したバットデータ |
| `img1.jpg`        | 元の入力画像             |

---

## ステップ 2：データの読み込み

```python
import pandas as pd

# CSVを読み込む
kp_df = pd.read_csv("/content/keypoints.csv")
bat_df = pd.read_csv("/content/bat_boxes.csv")

print("=== keypoints.csv ===")
print(kp_df.head())
print(f"\n人物数: {kp_df['person_id'].nunique()} 人")

print("\n=== bat_boxes.csv ===")
print(bat_df)
```

---

## ステップ 3：手首キーポイントの抽出

```python
# 手首のみ抽出（left_wrist / right_wrist）
wrist_df = kp_df[kp_df["keypoint"].isin(["left_wrist", "right_wrist"])].copy()

print("手首キーポイント一覧：")
print(wrist_df.to_string(index=False))
```

### 期待される出力例

```
 person_id     keypoint       x       y  confidence
         0  left_wrist  832.39  281.71      0.9948
         0 right_wrist  ...
         1  left_wrist  ...
         ...
```

---

## ステップ 4：バットとの距離計算

```python
import numpy as np

def point_to_box_distance(px, py, x1, y1, x2, y2):
    """点 (px, py) と矩形 (x1, y1, x2, y2) の最短距離を計算する"""
    dx = max(x1 - px, 0, px - x2)
    dy = max(y1 - py, 0, py - y2)
    return np.sqrt(dx**2 + dy**2)

# 各人物の最小距離を記録
person_ids = wrist_df["person_id"].unique()
results = []

for pid in person_ids:
    person_wrists = wrist_df[wrist_df["person_id"] == pid]
    min_dist = float("inf")

    for _, wrist in person_wrists.iterrows():
        wx, wy = wrist["x"], wrist["y"]
        for _, bat in bat_df.iterrows():
            dist = point_to_box_distance(wx, wy, bat["x1"], bat["y1"], bat["x2"], bat["y2"])
            if dist < min_dist:
                min_dist = dist

    results.append({"person_id": pid, "min_distance_to_bat": round(min_dist, 2)})

dist_df = pd.DataFrame(results).sort_values("min_distance_to_bat")
print(dist_df.to_string(index=False))
```

---

## ステップ 5：打者の特定

```python
batter_id = dist_df.iloc[0]["person_id"]
batter_dist = dist_df.iloc[0]["min_distance_to_bat"]

print(f"打者と判定された人物: person_id = {int(batter_id)}")
print(f"バットとの最短距離: {batter_dist} px")
```

---

## ステップ 6：結果の可視化

```python
import cv2
from google.colab.patches import cv2_imshow

img = cv2.imread("/content/img1.jpg")

# 全人物の手首を描画（青）
for _, row in wrist_df.iterrows():
    color = (0, 0, 255) if row["person_id"] == batter_id else (255, 0, 0)
    cv2.circle(img, (int(row["x"]), int(row["y"])), 10, color, -1)

# バットのバウンディングボックスを描画（緑）
for _, bat in bat_df.iterrows():
    cv2.rectangle(img, (int(bat["x1"]), int(bat["y1"])), (int(bat["x2"]), int(bat["y2"])), (0, 255, 0), 2)

# 打者ラベルを描画
batter_wrists = wrist_df[wrist_df["person_id"] == batter_id]
if not batter_wrists.empty:
    lx = int(batter_wrists.iloc[0]["x"])
    ly = int(batter_wrists.iloc[0]["y"]) - 20
    cv2.putText(img, f"BATTER (id={int(batter_id)})", (lx, ly),
                cv2.FONT_HERSHEY_SIMPLEX, 0.8, (0, 0, 255), 2)

cv2_imshow(img)
```

### 色の凡例

| 色 | 意味 |
|----|------|
| 赤 | 打者の手首 |
| 青 | その他の人物の手首 |
| 緑 | バットのバウンディングボックス |

---

## ステップ 7：結果のCSV保存（任意）

```python
OUTPUT_CSV = "/content/batter_result.csv"

dist_df["is_batter"] = dist_df["person_id"] == batter_id
dist_df.to_csv(OUTPUT_CSV, index=False)
print(f"保存しました: {OUTPUT_CSV}")
print(dist_df.to_string(index=False))
```

---

## バットが判定されない・間違える場合の対処

| 症状 | 原因 | 対処 |
|------|------|------|
| 判定が明らかに違う人物になる | バックネット越しのバット誤検出 | 信頼度でフィルタリング（bat_df = bat_df[bat_df["confidence"] > 0.5]） |
| 距離が大きい | 手首信頼度が低い | 信頼度フィルタ（wrist_df = wrist_df[wrist_df["confidence"] > 0.3]） |

---

## ステップ完了後のフォルダ構成

```
Step04 打者の自動特定/
├── 実装ステップ.md       ← 本ファイル
└── batter_result.csv      ← 各人物の距離・打者フラグ
```

---

## 次のステップ

| ステップ | 内容 |
| -------- | ---- |
| Step05   | 動画への応用：フレームごとにバット検出・打者追跡 |

---

## 参考リンク

- [Ultralytics YOLOv8 Pose 公式ドキュメント](https://docs.ultralytics.com/tasks/pose/)
- [OpenCV Drawing Functions](https://docs.opencv.org/4.x/dc/da5/tutorial_py_drawing_functions.html)
