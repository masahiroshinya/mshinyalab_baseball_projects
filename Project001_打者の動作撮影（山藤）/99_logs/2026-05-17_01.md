# 作業ログ: 2026-05-17

## 作業内容
1. **ChArUcoキャリブレーション処理のアップデート (OpenCV 4.7+ API対応)**
   - `cv2.aruco.detectMarkers` から `cv2.aruco.CharucoDetector` へ移行。
   - `cv2.aruco.calibrateCameraCharuco` の廃止に伴い、自前で `board.getChessboardCorners()` から3D座標を取得し、`cv2.calibrateCamera` へ渡す処理に変更。
2. **キャリブレーション精度の向上**
   - RMS再投影誤差の計算式を修正（ユークリッド距離の単純平均から、正しいRMSEに修正）。
   - アクションカメラ（OSMO Action 3等）の広角レンズに対応するため、`cv2.calibrateCamera` に `cv2.CALIB_RATIONAL_MODEL` フラグを追加。
3. **ステレオキャリブレーションおよび3D再構成の結合**
   - 2台のカメラに共通するChArUcoマーカーIDを抽出し、`cv2.stereoCalibrate` でカメラ間位置関係（R, T）を算出・保存（`stereo_calib_result.npz`）。
   - 分析区間として指定された全フレームに対してYOLOv8を回し、`cv2.triangulatePoints` を用いて3D座標を算出する一気通貫スクリプトを完成。
4. **分析フレーム指定UIのバグ修正**
   - ColabのUIスライダーから取得した開始・終了フレームが文字列（str）になっていたことによるTypeErrorを修正。
   - `range` を使って開始〜終了の全フレームを正しく処理対象リスト化（`target_frames`）するように修正。
5. **ドキュメントの更新**
   - `sandbox/Step09 連続画像からの姿勢推定・バット検出/実装ステップ2_csvからの姿勢推定と3D再構成.md` 内のコードブロックを、今回作成した最新コードへと更新。
6. **3D可視化（Plotly）**
   - 出力された `batting_3d_tracking.csv` を用いて、PlotlyによるInteractiveな3D骨格・バット描画（アニメーション再生機能付き）スクリプトを作成・実行確認。

## 次のステップ
- （もし必要なら）3D可視化スクリプトのドキュメント（実装ステップ3）の更新
- 実データの精度評価や、必要に応じた平滑化（スムージング）処理の検討
- 全体のワークフローの振り返りとブラッシュアップ
